---
plan: 01-02
phase: 1
wave: 1
depends_on: []
requirements: [SOC-03]
files_modified:
  - supabase/migrations/20260330120000_companies_legal_fields.sql
  - components/rh/ParametresForm.tsx
  - app/api/entreprise/route.ts
  - app/(dashboard)/paie/[id]/print/page.tsx
autonomous: false

must_haves:
  truths:
    - "Le bulletin imprimé affiche raison_sociale, adresse, cnps_matricule, nccm, ncc sans afficher '—'"
    - "Le formulaire Paramètres permet de saisir et enregistrer les 5 nouveaux champs légaux"
    - "PUT /api/entreprise accepte et persiste les 5 nouveaux champs"
  artifacts:
    - path: "supabase/migrations/20260330120000_companies_legal_fields.sql"
      provides: "Migration SQL ajoutant raison_sociale, adresse, cnps_matricule, nccm, ncc sur companies"
      contains: "ADD COLUMN IF NOT EXISTS raison_sociale"
    - path: "components/rh/ParametresForm.tsx"
      provides: "Formulaire avec les 5 nouveaux champs"
    - path: "app/api/entreprise/route.ts"
      provides: "Route PUT acceptant et persistant les 5 nouveaux champs"
  key_links:
    - from: "components/rh/ParametresForm.tsx"
      to: "app/api/entreprise/route.ts"
      via: "fetch PUT /api/entreprise avec les 5 nouveaux champs"
      pattern: "PUT.*api/entreprise"
    - from: "app/api/entreprise/route.ts"
      to: "supabase companies table"
      via: ".update({ raison_sociale, adresse, cnps_matricule, nccm, ncc })"
      pattern: "raison_sociale"
    - from: "app/(dashboard)/paie/[id]/print/page.tsx"
      to: "companies.raison_sociale"
      via: "lecture directe via createServerClient().from('companies').select('raison_sociale,...')"
      pattern: "raison_sociale"
---

<objective>
Ajouter les colonnes légales manquantes sur la table companies (raison_sociale, adresse, cnps_matricule, nccm, ncc), mettre à jour le formulaire Paramètres pour les saisir, la route API pour les persister, et le bulletin imprimé pour les afficher correctement sans les "—" actuels.

Purpose: Le bulletin de paie est le document légal principal du SaaS. Sans le nom légal, l'adresse et les numéros officiels de l'entreprise, le bulletin n'est pas conforme au droit ivoirien.
Output: Migration SQL + formulaire Paramètres mis à jour + bulletin imprimé conforme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-stabilisation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Migration SQL + mise à jour API entreprise</name>
  <files>
    supabase/migrations/20260330120000_companies_legal_fields.sql,
    app/api/entreprise/route.ts
  </files>
  <read_first>
    - app/api/entreprise/route.ts
    - types/supabase.ts (section companies)
  </read_first>
  <action>
  ETAPE 1 — Créer le dossier supabase/migrations/ s'il n'existe pas, puis créer le fichier de migration :

  Fichier : supabase/migrations/20260330120000_companies_legal_fields.sql
  Contenu exact :
  ```sql
  -- Migration : ajout des champs légaux sur companies pour le bulletin de paie CI
  -- Date : 2026-03-30
  ALTER TABLE companies
    ADD COLUMN IF NOT EXISTS raison_sociale  VARCHAR(200),
    ADD COLUMN IF NOT EXISTS adresse         TEXT,
    ADD COLUMN IF NOT EXISTS cnps_matricule  VARCHAR(30),
    ADD COLUMN IF NOT EXISTS nccm            VARCHAR(30),
    ADD COLUMN IF NOT EXISTS ncc             VARCHAR(30);

  COMMENT ON COLUMN companies.raison_sociale  IS 'Dénomination sociale légale — affiché sur le bulletin de paie';
  COMMENT ON COLUMN companies.cnps_matricule  IS 'Numéro matricule CNPS employeur CI';
  COMMENT ON COLUMN companies.nccm            IS 'Numéro de Compte Cotisant Maladie CNAM';
  COMMENT ON COLUMN companies.ncc             IS 'Numéro de Compte Contribuable DGI CI';
  ```

  ETAPE 2 — Appliquer la migration dans Supabase Dashboard SQL Editor.
  Exécuter le contenu du fichier supabase/migrations/20260330120000_companies_legal_fields.sql dans le SQL Editor Supabase.
  Vérifier : SELECT column_name FROM information_schema.columns WHERE table_name = 'companies' AND column_name IN ('raison_sociale', 'adresse', 'cnps_matricule', 'nccm', 'ncc');

  ETAPE 3 — Mettre à jour app/api/entreprise/route.ts.
  Lire le fichier actuel. Localiser le schéma Zod (entrepriseSchema ou similaire). Étendre avec les 5 nouveaux champs en appliquant la transformation null pour les chaînes vides :

  ```typescript
  // Ajouter dans le schéma Zod existant (conserver les champs name et convention_collective)
  raison_sociale: z.string().max(200).nullable().optional()
    .transform(v => (v === "" ? null : v)),
  adresse: z.string().nullable().optional()
    .transform(v => (v === "" ? null : v)),
  cnps_matricule: z.string().max(30).nullable().optional()
    .transform(v => (v === "" ? null : v)),
  nccm: z.string().max(30).nullable().optional()
    .transform(v => (v === "" ? null : v)),
  ncc: z.string().max(30).nullable().optional()
    .transform(v => (v === "" ? null : v)),
  ```

  Dans le handler PUT, inclure les 5 nouveaux champs dans l'objet passé à .update(). La query Supabase existante doit rester filtrée par company_id via RLS (pas de .eq() explicite nécessaire car la RLS companies est USING (id = get_user_company_id())).

  Ne pas utiliser `any`. Utiliser `Tables<'companies'>` depuis types/supabase.ts si les types sont déjà régénérés (plan 01-01 wave 1 parallèle). Si types/supabase.ts n'a pas encore ces colonnes, déclarer les nouveaux champs avec un type local temporaire `{ raison_sociale?: string | null; ... }` — ils seront remplacés après régénération.
  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && grep -c "raison_sociale" supabase/migrations/20260330120000_companies_legal_fields.sql && grep -c "raison_sociale" app/api/entreprise/route.ts && echo "OK: migration et route contiennent raison_sociale"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier supabase/migrations/20260330120000_companies_legal_fields.sql existe et contient "ADD COLUMN IF NOT EXISTS raison_sociale"
    - grep -c "raison_sociale" app/api/entreprise/route.ts retourne > 0
    - grep -c "cnps_matricule" app/api/entreprise/route.ts retourne > 0
    - Le schéma Zod dans app/api/entreprise/route.ts contient les 5 nouveaux champs avec .transform(v => (v === "" ? null : v))
    - npx tsc --noEmit ne signale aucune erreur dans app/api/entreprise/route.ts
  </acceptance_criteria>
  <done>Migration SQL créée avec les 5 colonnes. Route PUT /api/entreprise accepte et persiste raison_sociale, adresse, cnps_matricule, nccm, ncc avec transformation "" → null.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Formulaire ParametresForm + affichage bulletin</name>
  <files>
    components/rh/ParametresForm.tsx,
    app/(dashboard)/paie/[id]/print/page.tsx
  </files>
  <read_first>
    - components/rh/ParametresForm.tsx
    - app/(dashboard)/paie/[id]/print/page.tsx
    - app/(dashboard)/parametres/page.tsx
  </read_first>
  <action>
  ETAPE 1 — Mettre à jour components/rh/ParametresForm.tsx.
  Lire le composant actuel. Il gère name et convention_collective. Ajouter les 5 champs :

  Dans le type/interface des valeurs du formulaire (ou inférence Zod), ajouter :
  ```typescript
  raison_sociale?: string | null
  adresse?: string | null
  cnps_matricule?: string | null
  nccm?: string | null
  ncc?: string | null
  ```

  Dans le defaultValues du useForm (ou équivalent), initialiser depuis les données entreprise reçues en props ou chargées depuis l'API.

  Ajouter dans le JSX, après le champ convention_collective, une section "Informations légales" avec 5 champs Input shadcn/ui :
  - Raison sociale (label: "Raison sociale", name: "raison_sociale", placeholder: "ex: SARL GRAVEL IVOIRE")
  - Adresse du siège (label: "Adresse du siège", name: "adresse", placeholder: "ex: Abidjan, Plateau, Rue des Jardins")
  - Matricule CNPS (label: "Matricule CNPS employeur", name: "cnps_matricule", placeholder: "ex: 123456789")
  - N° NCCM (label: "N° Compte Cotisant Maladie", name: "nccm", placeholder: "ex: 0012345")
  - N° NCC (label: "N° Compte Contribuable", name: "ncc", placeholder: "ex: CI-ABJ-2025-000123")

  Tous les 5 champs sont optionnels (pas de required). Utiliser les composants Input de components/ui/input.tsx avec React.forwardRef pour la compatibilité react-hook-form.

  Dans la fonction de soumission du formulaire (fetch vers PUT /api/entreprise), inclure les 5 nouveaux champs dans le body JSON.

  ETAPE 2 — Mettre à jour app/(dashboard)/paie/[id]/print/page.tsx.
  Lire le fichier actuel. Localiser la requête sur companies (ligne ~147-156 d'après CONCERNS.md). Cette requête lit déjà raison_sociale, adresse, cnps_matricule, nccm, ncc via un cast `as Record<string, string>`. Avec les types régénérés (plan 01-01), remplacer ce cast par `Tables<'companies'>`.

  Vérifier que l'affichage des valeurs gère correctement le cas null :
  ```tsx
  {company.raison_sociale ?? company.name}  // Fallback sur name si raison_sociale non renseigné
  {company.adresse ?? "—"}
  {company.cnps_matricule ?? "—"}
  {company.nccm ?? "—"}
  {company.ncc ?? "—"}
  ```

  S'assurer que le SELECT sur companies dans cette page inclut les 5 nouveaux champs :
  ```typescript
  .select('id, name, raison_sociale, adresse, cnps_matricule, nccm, ncc, convention_collective')
  ```
  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && grep -c "raison_sociale" components/rh/ParametresForm.tsx && grep -c "cnps_matricule" app/\(dashboard\)/paie/\[id\]/print/page.tsx && npx tsc --noEmit 2>&1 | grep -c "ParametresForm\|print" | head -1 && echo "OK: formulaire et bulletin mis à jour"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "raison_sociale" components/rh/ParametresForm.tsx retourne > 0
    - grep -c "cnps_matricule" components/rh/ParametresForm.tsx retourne > 0
    - grep -c "raison_sociale" app/(dashboard)/paie/[id]/print/page.tsx retourne > 0
    - Le select Supabase dans print/page.tsx inclut 'raison_sociale, adresse, cnps_matricule, nccm, ncc'
    - npx tsc --noEmit ne signale aucune erreur dans ParametresForm.tsx ni print/page.tsx
    - Aucun cast "as Record<string, string>" sur l'objet company dans print/page.tsx
  </acceptance_criteria>
  <done>ParametresForm.tsx expose les 5 champs légaux saisissables. print/page.tsx lit et affiche raison_sociale, adresse, cnps_matricule, nccm, ncc depuis la base sans cast dangereux.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : Vérification visuelle du bulletin imprimé</name>
  <what-built>
  Migration SQL appliquée, 5 nouveaux champs dans Paramètres et dans le bulletin imprimé.
  </what-built>
  <how-to-verify>
  1. Démarrer l'application : npm run dev
  2. Aller sur /parametres
  3. Renseigner : Raison sociale "TEST SARL", Adresse "Abidjan, Plateau", Matricule CNPS "999888777", N° NCCM "0099887", N° NCC "CI-ABJ-2026-TEST"
  4. Enregistrer les paramètres (bouton Sauvegarder ou équivalent)
  5. Aller sur /paie, ouvrir un bulletin existant, cliquer sur "Imprimer" ou naviguer vers /paie/[id]/print
  6. Vérifier que l'en-tête du bulletin affiche :
     - "TEST SARL" (ou la raison sociale saisie) à la place du nom générique
     - "Abidjan, Plateau" comme adresse
     - "999888777" comme numéro CNPS
     - "0099887" comme NCCM
     - "CI-ABJ-2026-TEST" comme NCC
  7. Confirmer qu'aucun champ n'affiche "—" pour ces valeurs
  </how-to-verify>
  <resume-signal>Tapez "bulletin ok" si tous les champs s'affichent correctement, ou décrivez les problèmes observés.</resume-signal>
</task>

</tasks>

<verification>
Après les trois tâches :
1. SELECT raison_sociale, adresse, cnps_matricule, nccm, ncc FROM companies LIMIT 1 — retourne les valeurs saisies (pas NULL)
2. Le bulletin imprimé /paie/[id]/print affiche toutes les données légales sans "—"
3. npx tsc --noEmit — aucune erreur dans les fichiers modifiés
</verification>

<success_criteria>
- La table companies a 5 nouvelles colonnes en production (raison_sociale, adresse, cnps_matricule, nccm, ncc)
- Le formulaire /parametres permet de saisir et enregistrer ces données
- Le bulletin imprimé affiche les données légales correctement
- SOC-03 satisfait : bulletin conforme avec données entreprise complètes
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-stabilisation/01-02-SUMMARY.md` avec :
- Colonnes ajoutées à companies
- Fichiers modifiés
- Résultat de la vérification visuelle du bulletin
</output>
