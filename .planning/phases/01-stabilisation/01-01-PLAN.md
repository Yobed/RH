---
plan: 01-01
phase: 1
wave: 1
depends_on: []
requirements: [SOC-01]
files_modified:
  - types/supabase.ts
  - app/(dashboard)/paie/[id]/print/page.tsx
  - app/api/notifications/sync/route.ts
  - app/(dashboard)/employes/[id]/page.tsx
  - app/api/employees/[id]/route.ts
autonomous: true

must_haves:
  truths:
    - "npx tsc --noEmit se termine sans erreur après la régénération"
    - "Le fichier types/supabase.ts contient les types de bulletins_paie, employee_salary_history, et toutes les colonnes primes des employees"
    - "Aucune occurrence de 'as unknown as' ne subsiste dans les 15 fichiers applicatifs (hors node_modules)"
  artifacts:
    - path: "types/supabase.ts"
      provides: "Types complets — bulletins_paie, employee_salary_history, employees avec primes, companies"
      contains: "bulletins_paie"
    - path: "app/(dashboard)/paie/[id]/print/page.tsx"
      provides: "Page bulletin sans cast TypeScript"
  key_links:
    - from: "types/supabase.ts"
      to: "app/(dashboard)/paie/[id]/print/page.tsx"
      via: "Tables<'bulletins_paie'> et Tables<'companies'>"
      pattern: "Tables<'bulletins_paie'>"
    - from: "types/supabase.ts"
      to: "app/api/employees/[id]/route.ts"
      via: "Tables<'employee_salary_history'> — supprime le cast (supabase.from as ...)"
      pattern: "employee_salary_history"
---

<objective>
Synchroniser types/supabase.ts avec l'état réel de la base de données Supabase en appliquant les 5 migrations SQL existantes (si pas encore appliquées en production) puis en régénérant les types via Supabase CLI. Supprimer ensuite les 38 occurrences de `as unknown as` dans les 15 fichiers applicatifs qui compensaient les types manquants.

Purpose: Les types désynchronisés obligent le code à contourner TypeScript avec des casts dangereux. Une fois les types corrects, ces contournements disparaissent et les erreurs de compilation révèlent les vrais problèmes.
Output: types/supabase.ts complet + compilation TypeScript propre (0 as unknown as dans le code applicatif).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-stabilisation/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1 : Vérifier et appliquer les migrations SQL en production</name>
  <files>scripts/migration_securite.sql, scripts/add_employee_fields.sql, scripts/add_employee_primes.sql, scripts/add_bulletin_primes.sql, scripts/add_salary_history.sql</files>
  <read_first>
    - scripts/migration_securite.sql
    - scripts/add_employee_fields.sql
    - scripts/add_employee_primes.sql
    - scripts/add_bulletin_primes.sql
    - scripts/add_salary_history.sql
  </read_first>
  <action>
  Cette tâche nécessite une action manuelle dans Supabase Dashboard car l'état exact des migrations en production est inconnu.

  ETAPE 1 — Vérifier quelles colonnes existent déjà.
  Dans le SQL Editor Supabase (<https://supabase.com/dashboard/project/[votre-projet]/sql>), exécuter ces requêtes de vérification :

  ```sql
  -- Vérifier les colonnes de employees
  SELECT column_name FROM information_schema.columns
  WHERE table_schema = 'public' AND table_name = 'employees'
  ORDER BY ordinal_position;

  -- Vérifier si bulletins_paie existe
  SELECT table_name FROM information_schema.tables
  WHERE table_schema = 'public' AND table_name = 'bulletins_paie';

  -- Vérifier si employee_salary_history existe
  SELECT table_name FROM information_schema.tables
  WHERE table_schema = 'public' AND table_name = 'employee_salary_history';
  ```

  ETAPE 2 — Pour chaque script dont les colonnes/tables sont ABSENTES, exécuter le script SQL correspondant dans le SQL Editor Supabase, dans cet ordre strict :

  1. migration_securite.sql (si audit_logs est absent)
  2. add_employee_fields.sql (si civilite, nationalite, etc. sont absents)
  3. add_bulletin_primes.sql (si bulletins_paie ou num_cnps sont absents)
  4. add_employee_primes.sql (si sursalaire, prime_exceptionnelle sont absents)
  5. add_salary_history.sql (si employee_salary_history est absent)

  ETAPE 3 — Confirmer que toutes les colonnes et tables existent en réexécutant les requêtes de vérification.
  </action>
  <how-to-verify>
  Toutes ces requêtes doivent retourner des résultats non vides :

- SELECT column_name FROM information_schema.columns WHERE table_name = 'employees' AND column_name = 'sursalaire';
- SELECT table_name FROM information_schema.tables WHERE table_name = 'bulletins_paie';
- SELECT table_name FROM information_schema.tables WHERE table_name = 'employee_salary_history';
  </how-to-verify>

  <resume-signal>Tapez "migrations ok" une fois que toutes les tables et colonnes sont confirmées en production.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Régénérer types/supabase.ts et supprimer tous les as unknown as</name>
  <files>
    types/supabase.ts,
    app/(dashboard)/paie/[id]/print/page.tsx,
    app/api/notifications/sync/route.ts,
    app/(dashboard)/employes/[id]/page.tsx,
    app/api/employees/[id]/route.ts,
    components/rh/PaieDialog.tsx,
    components/rh/EmployeeDialog.tsx,
    app/api/paie/route.ts,
    app/api/paie/[id]/route.ts,
    app/api/employees/route.ts
  </files>
  <read_first>
    - types/supabase.ts
    - app/(dashboard)/paie/[id]/print/page.tsx
    - app/api/notifications/sync/route.ts
    - app/(dashboard)/employes/[id]/page.tsx
    - app/api/employees/[id]/route.ts
    - .env.local (pour extraire NEXT_PUBLIC_SUPABASE_URL)
  </read_first>
  <action>
  ETAPE 1 — Extraire le PROJECT_REF depuis NEXT_PUBLIC_SUPABASE_URL.
  La valeur est dans .env.local, format : https://[PROJECT_REF].supabase.co
  Extraire la partie PROJECT_REF (20 caractères alphanumériques).

  ETAPE 2 — Régénérer les types :

  ```bash
  cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine"
  npx supabase gen types typescript --project-id [PROJECT_REF] --schema public > types/supabase.ts
  ```

  ETAPE 3 — Vérifier que la régénération est complète. Le fichier types/supabase.ts doit contenir :

- La table "bulletins_paie" avec ses colonnes (sursalaire, prime_anciennete, prime_exceptionnelle, etc.)
- La table "employee_salary_history"
- Les colonnes primes dans "employees" (sursalaire, prime_exceptionnelle, prime_salissure, prime_depassement, prime_fonction, prime_transport, civilite, nationalite, etat_civil, nb_enfants, niveau_etude, categorie, num_cnps)

  ETAPE 4 — Supprimer tous les `as unknown as` dans le code applicatif.
  Localiser avec : grep -rn "as unknown as" app/ components/ lib/ --include="*.ts" --include="*.tsx"

  Pour chaque occurrence :

- app/(dashboard)/paie/[id]/print/page.tsx : remplacer les casts par `Tables<'bulletins_paie'>` et `Tables<'companies'>` maintenant que ces types existent
- app/api/notifications/sync/route.ts : remplacer les casts par les types Supabase appropriés
- app/(dashboard)/employes/[id]/page.tsx : remplacer les casts sur les champs `civilite`, `nationalite`, etc. par `Tables<'employees'>`
- app/api/employees/[id]/route.ts : supprimer le contournement `(supabase.from as (t: string) => ...)("employee_salary_history")` et utiliser directement `supabase.from("employee_salary_history")`

  ETAPE 5 — Compiler et corriger les erreurs TypeScript résiduelles :

  ```bash
  cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine"
  npx tsc --noEmit 2>&1 | head -50
  ```

  Corriger chaque erreur signalée jusqu'à compilation propre. Ne jamais utiliser `any` pour résoudre une erreur.

  ETAPE 6 — Vérifier zéro occurrence :

  ```bash
  grep -rn "as unknown as" app/ components/ lib/ --include="*.ts" --include="*.tsx" | grep -v node_modules
  ```

  Cette commande doit retourner aucune ligne.
  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && npx tsc --noEmit && grep -rn "as unknown as" app/ components/ lib/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | wc -l | grep -q "^0$" && echo "OK: zero as unknown as, TypeScript propre"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "as unknown as" app/ components/ lib/ --include="*.ts" --include="*.tsx" | grep -v node_modules retourne 0 lignes
    - npx tsc --noEmit se termine avec exit code 0 (aucune erreur)
    - grep -c "bulletins_paie" types/supabase.ts retourne un nombre > 0
    - grep -c "employee_salary_history" types/supabase.ts retourne un nombre > 0
    - grep -c "sursalaire" types/supabase.ts retourne un nombre > 0
  </acceptance_criteria>
  <done>types/supabase.ts contient les types complets pour bulletins_paie, employee_salary_history, et toutes les colonnes primes de employees. npx tsc --noEmit passe. Zéro occurrence de "as unknown as" dans app/, components/, lib/.</done>
</task>

</tasks>

<verification>
Après les deux tâches :
1. npx tsc --noEmit — doit se terminer sans erreur (exit code 0)
2. grep -rn "as unknown as" app/ components/ lib/ --include="*.ts" --include="*.tsx" | grep -v node_modules — doit retourner 0 résultat
3. grep "bulletins_paie" types/supabase.ts — doit retourner des lignes de type
4. grep "employee_salary_history" types/supabase.ts — doit retourner des lignes de type
</verification>

<success_criteria>

- types/supabase.ts est synchronisé avec le schéma Supabase en production
- Zéro occurrence de "as unknown as" dans le code applicatif (15 fichiers concernés)
- npx tsc --noEmit passe sans erreur
- SOC-01 satisfait : types synchronisés avec toutes les migrations
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-stabilisation/01-01-SUMMARY.md` avec :
- Colonnes/tables ajoutées aux types
- Nombre exact d'occurrences "as unknown as" supprimées
- Fichiers modifiés
- Résultat npx tsc --noEmit
</output>
