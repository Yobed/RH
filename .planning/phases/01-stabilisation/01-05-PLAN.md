---
plan: 01-05
phase: 1
wave: 1
depends_on: []
requirements: [SOC-01, SOC-02, SOC-03, SOC-04]
files_modified:
  - app/api/rag/upload/route.ts
  - app/api/paie/route.ts
  - app/api/paie/[id]/route.ts
  - supabase/migrations/20260324120000_securite_rls.sql
  - supabase/migrations/20260325000000_employee_fields.sql
  - supabase/migrations/20260326090000_bulletin_primes.sql
  - supabase/migrations/20260326094300_employee_primes.sql
  - supabase/migrations/20260326100700_salary_history.sql
autonomous: true

must_haves:
  truths:
    - "POST /api/rag/upload retourne 403 si l'utilisateur authentifié n'a pas profile.role = 'admin'"
    - "La table audit_logs reçoit un INSERT après CREATE_BULLETIN, UPDATE_BULLETIN_STATUT, et RAG_UPLOAD"
    - "Le dossier supabase/migrations/ contient les 5 scripts historiques renommés avec horodatage YYYYMMDDHHMMSS"
  artifacts:
    - path: "app/api/rag/upload/route.ts"
      provides: "Vérification rôle admin avant upload RAG"
      contains: "profile?.role !== \"admin\""
    - path: "app/api/paie/route.ts"
      provides: "INSERT dans audit_logs après POST (CREATE_BULLETIN)"
      contains: "CREATE_BULLETIN"
    - path: "app/api/paie/[id]/route.ts"
      provides: "INSERT dans audit_logs après PATCH (UPDATE_BULLETIN_STATUT)"
      contains: "UPDATE_BULLETIN_STATUT"
    - path: "supabase/migrations/20260324120000_securite_rls.sql"
      provides: "Migration versionnée — securite RLS"
  key_links:
    - from: "app/api/rag/upload/route.ts"
      to: "supabase profiles table"
      via: ".from('profiles').select('role').eq('id', user.id).single()"
      pattern: "profiles.*role"
    - from: "app/api/paie/route.ts"
      to: "supabase audit_logs table"
      via: ".from('audit_logs').insert({ action: 'CREATE_BULLETIN', ... })"
      pattern: "audit_logs.*CREATE_BULLETIN"
---

<objective>
Sécuriser POST /api/rag/upload avec vérification du rôle admin, alimenter la table audit_logs sur les 3 opérations critiques (RAG_UPLOAD, CREATE_BULLETIN, UPDATE_BULLETIN_STATUT), et versionner les 5 migrations SQL historiques dans supabase/migrations/ avec la convention YYYYMMDDHHMMSS.

Purpose: Tout utilisateur authentifié peut actuellement empoisonner la base RAG partagée avec de faux textes légaux — risque avéré documenté dans CONCERNS.md #10. L'audit_logs est vide malgré sa création — impossible de tracer qui a modifié quoi dans un SaaS multi-tenant.
Output: POST /api/rag/upload sécurisé + 3 points d'audit + 5 migrations versionnées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-stabilisation/01-RESEARCH.md
</context>

<interfaces>
<!-- Pattern de vérification rôle admin — pattern existant dans le projet -->
<!-- Lire app/api/conges/route.ts pour voir comment profiles.role est déjà consulté -->

Schéma audit_logs (depuis types/supabase.ts) :
```typescript
// audit_logs.Insert
{
  action: string,      // "CREATE_BULLETIN" | "UPDATE_BULLETIN_STATUT" | "RAG_UPLOAD"
  company_id: string,  // UUID de l'entreprise
  user_id: string,     // auth.uid()
  resource?: string | null,  // ex: "bulletins_paie:uuid" ou "legal_documents:uuid"
}
```

Pattern multi-tenant existant dans les routes :
```typescript
const { data: { user } } = await supabase.auth.getUser()
if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 })
const { data: companyId } = await supabase.rpc('get_user_company_id')
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Versionner les migrations SQL dans supabase/migrations/</name>
  <files>
    supabase/migrations/20260324120000_securite_rls.sql,
    supabase/migrations/20260325000000_employee_fields.sql,
    supabase/migrations/20260326090000_bulletin_primes.sql,
    supabase/migrations/20260326094300_employee_primes.sql,
    supabase/migrations/20260326100700_salary_history.sql
  </files>
  <read_first>
    - scripts/migration_securite.sql
    - scripts/add_employee_fields.sql
    - scripts/add_bulletin_primes.sql
    - scripts/add_employee_primes.sql
    - scripts/add_salary_history.sql
  </read_first>
  <action>
  ETAPE 1 — Vérifier que le dossier supabase/migrations/ existe. Si non, le créer :
  ```bash
  mkdir -p "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine/supabase/migrations"
  ```

  ETAPE 2 — Lire chaque script dans scripts/ et créer la copie versionnée dans supabase/migrations/ avec le contenu exact (ne pas modifier le SQL — copie fidèle) :

  Correspondances de nommage :
  - scripts/migration_securite.sql → supabase/migrations/20260324120000_securite_rls.sql
  - scripts/add_employee_fields.sql → supabase/migrations/20260325000000_employee_fields.sql
  - scripts/add_bulletin_primes.sql → supabase/migrations/20260326090000_bulletin_primes.sql
  - scripts/add_employee_primes.sql → supabase/migrations/20260326094300_employee_primes.sql
  - scripts/add_salary_history.sql → supabase/migrations/20260326100700_salary_history.sql

  Ajouter en tête de chaque fichier copié un commentaire indiquant l'origine :
  ```sql
  -- Migration versionnée depuis scripts/[nom_original].sql
  -- Appliquée manuellement en production (date approximative : [date_dans_le_nom])
  -- NE PAS réexécuter si les colonnes/tables existent déjà (scripts avec IF NOT EXISTS)
  ```

  ETAPE 3 — Vérifier que les 5 fichiers existent :
  ```bash
  ls "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine/supabase/migrations/"
  ```

  Note : Ne PAS supprimer les scripts originaux dans scripts/ — ils servent de référence et certains (add_employee_primes.sql) sont non commités (untracked).
  </action>
  <verify>
    <automated>ls "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine/supabase/migrations/" | grep -c "\.sql" | grep -q "^5$\|^6$" && echo "OK: migrations versionnées présentes"</automated>
  </verify>
  <acceptance_criteria>
    - ls supabase/migrations/ liste au moins 5 fichiers .sql
    - Chaque fichier commence par un timestamp 14 chiffres (YYYYMMDDHHMMSS)
    - Le contenu de chaque fichier correspond au script original dans scripts/
    - grep -c "securite_rls\|employee_fields\|bulletin_primes\|employee_primes\|salary_history" indique les 5 migrations présentes
  </acceptance_criteria>
  <done>5 fichiers dans supabase/migrations/ avec nommage YYYYMMDDHHMMSS_description.sql. Contenu identique aux scripts originaux avec commentaire d'en-tête ajouté.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Sécuriser POST /api/rag/upload + alimenter audit_logs (3 points)</name>
  <files>
    app/api/rag/upload/route.ts,
    app/api/paie/route.ts,
    app/api/paie/[id]/route.ts
  </files>
  <read_first>
    - app/api/rag/upload/route.ts (entier — pour localiser où insérer la vérification du rôle)
    - app/api/paie/route.ts (entier — pour localiser le POST CREATE_BULLETIN)
    - app/api/paie/[id]/route.ts (entier — pour localiser le PATCH UPDATE_BULLETIN)
    - app/api/conges/route.ts (pour lire le pattern existant de consultation de profiles.role)
  </read_first>
  <action>
  ETAPE 1 — Sécuriser app/api/rag/upload/route.ts.

  Lire le fichier en entier. Localiser le handler POST. Après la vérification de l'utilisateur (supabase.auth.getUser()), ajouter immédiatement la vérification du rôle admin avant toute autre logique :

  ```typescript
  // Vérification rôle admin — seuls les admins peuvent alimenter la base RAG partagée
  const { data: profile } = await supabase
    .from("profiles")
    .select("role")
    .eq("id", user.id)
    .single()

  if (profile?.role !== "admin") {
    return NextResponse.json(
      { error: "Accès réservé aux administrateurs" },
      { status: 403 }
    )
  }
  ```

  Après l'insertion réussie dans legal_documents (localiser le .insert dans le code existant), ajouter le log d'audit :
  ```typescript
  // Log d'audit après upload RAG réussi
  const chunkId = insertedData?.id ?? "unknown" // adapter selon la variable retournée par l'insert
  await supabase.from("audit_logs").insert({
    action: "RAG_UPLOAD",
    company_id: companyId as string,  // utiliser get_user_company_id() si pas déjà fait
    user_id: user.id,
    resource: `legal_documents:${chunkId}`,
  })
  ```

  Note sur la gestion du company_id dans rag/upload : d'après ARCHITECTURE.md, legal_documents est inséré avec company_id: null (documents partagés). Pour l'audit_log, utiliser quand même le company_id de l'admin qui fait l'upload via get_user_company_id(). Si aucun company_id n'est résolu (cas exceptionnel), passer la chaîne vide ou gérer l'erreur silencieusement (l'audit ne doit pas bloquer l'upload).

  ETAPE 2 — Alimenter audit_logs dans app/api/paie/route.ts (CREATE_BULLETIN).

  Lire le fichier. Localiser le handler POST (création d'un nouveau bulletin). Après l'insertion réussie dans bulletins_paie, ajouter :
  ```typescript
  if (newBulletin?.id) {
    // Audit : création d'un bulletin de paie
    await supabase.from("audit_logs").insert({
      action: "CREATE_BULLETIN",
      company_id: companyId as string,
      user_id: user.id,
      resource: `bulletins_paie:${newBulletin.id}`,
    })
  }
  ```
  Adapter `newBulletin` selon le nom exact de la variable retournée par l'insert. Ne pas bloquer la réponse si l'insert audit échoue — utiliser un try/catch séparé ou ignorer l'erreur d'audit.

  ETAPE 3 — Alimenter audit_logs dans app/api/paie/[id]/route.ts (UPDATE_BULLETIN_STATUT).

  Lire le fichier. Localiser le handler PATCH (modification du bulletin existant). Après la mise à jour réussie, ajouter :
  ```typescript
  await supabase.from("audit_logs").insert({
    action: "UPDATE_BULLETIN_STATUT",
    company_id: companyId as string,
    user_id: user.id,
    resource: `bulletins_paie:${params.id}`,
  })
  ```

  ETAPE 4 — Vérifier la compilation :
  ```bash
  cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine"
  npx tsc --noEmit 2>&1 | grep -E "rag/upload|api/paie"
  ```
  Corriger toute erreur TypeScript dans ces fichiers. Ne jamais utiliser `any`.
  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && grep -c "profile?.role !== \"admin\"" app/api/rag/upload/route.ts && grep -c "CREATE_BULLETIN" app/api/paie/route.ts && grep -c "UPDATE_BULLETIN_STATUT" app/api/paie/\[id\]/route.ts && npx tsc --noEmit && echo "OK: sécurité RAG + audit_logs + TypeScript propre"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "profile?.role !== \"admin\"" app/api/rag/upload/route.ts retourne >= 1
    - grep -c "403" app/api/rag/upload/route.ts retourne >= 1 (retour 403 si pas admin)
    - grep -c "CREATE_BULLETIN" app/api/paie/route.ts retourne >= 1
    - grep -c "UPDATE_BULLETIN_STATUT" app/api/paie/[id]/route.ts retourne >= 1
    - grep -c "RAG_UPLOAD" app/api/rag/upload/route.ts retourne >= 1
    - npx tsc --noEmit retourne exit code 0
    - Aucune occurrence de "any" introduite dans les fichiers modifiés
  </acceptance_criteria>
  <done>POST /api/rag/upload retourne 403 si profile.role !== 'admin'. audit_logs reçoit CREATE_BULLETIN, UPDATE_BULLETIN_STATUT, RAG_UPLOAD après chaque opération réussie. TypeScript propre dans les 3 fichiers.</done>
</task>

</tasks>

<verification>
Après les deux tâches :
1. Test sécurité RAG (si possible avec un compte non-admin) : curl -X POST /api/rag/upload avec token non-admin → doit retourner {"error":"Accès réservé aux administrateurs"} HTTP 403
2. ls supabase/migrations/ | wc -l — doit retourner >= 5
3. npx tsc --noEmit — doit passer sans erreur
4. grep -rn "audit_logs" app/api/ — doit retourner des résultats dans route.ts de paie et rag
</verification>

<success_criteria>
- POST /api/rag/upload bloqué à 403 pour les non-admins
- 3 points d'audit opérationnels (RAG_UPLOAD, CREATE_BULLETIN, UPDATE_BULLETIN_STATUT)
- 5 migrations versionnées dans supabase/migrations/
- npx tsc --noEmit passe
- Phase 1 complète : SOC-01 à SOC-04 tous adressés entre les plans 01-01 à 01-05
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-stabilisation/01-05-SUMMARY.md` avec :
- Fichiers de migrations créés dans supabase/migrations/
- Points d'audit implémentés (actions + fichiers)
- Confirmation de la vérification rôle admin
- Résultat npx tsc --noEmit
</output>
