---
plan: 01-03
phase: 1
wave: 2
depends_on: [01-01]
requirements: [SOC-02]
files_modified:
  - lib/paie-ci.ts
  - components/rh/PaieDialog.tsx
  - app/api/paie/[id]/route.ts
  - components/rh/EmployeeDialog.tsx
  - lib/utils-rh.ts
autonomous: true

must_haves:
  truths:
    - "previewCalc n'existe plus dans PaieDialog.tsx — la preview appelle calculerBulletinComplet() depuis lib/paie-ci.ts"
    - "PUT /api/paie/[id] filtre par company_id en plus de l'id du bulletin"
    - "EmployeeDialog.tsx initialise date_fin_contrat avec la valeur existante en mode édition (pas une chaîne vide)"
    - "formatAnciennete est exportée depuis lib/paie-ci.ts et importée depuis les pages qui l'utilisent"
    - "scoreLabel et scoreVariant sont exportées depuis lib/utils-rh.ts (nouveau fichier)"
  artifacts:
    - path: "lib/paie-ci.ts"
      provides: "calculerBulletinComplet(lignes: LignesBulletin): ResultatPaieComplet + formatAnciennete exportée"
      exports: ["calculerBulletinComplet", "LignesBulletin", "ResultatPaieComplet", "formatAnciennete"]
    - path: "lib/utils-rh.ts"
      provides: "scoreLabel et scoreVariant partagés"
      exports: ["scoreLabel", "scoreVariant"]
    - path: "components/rh/PaieDialog.tsx"
      provides: "Preview utilisant calculerBulletinComplet — sans previewCalc local"
    - path: "app/api/paie/[id]/route.ts"
      provides: "PUT sécurisé avec filtre company_id"
  key_links:
    - from: "components/rh/PaieDialog.tsx"
      to: "lib/paie-ci.ts"
      via: "import { calculerBulletinComplet } from '@/lib/paie-ci'"
      pattern: "calculerBulletinComplet"
    - from: "app/api/paie/[id]/route.ts"
      to: "supabase.rpc('get_user_company_id')"
      via: "filtre .eq('company_id', companyId) dans le PUT"
      pattern: "company_id.*companyId"
---

<objective>
Centraliser le calcul de paie dans lib/paie-ci.ts en créant calculerBulletinComplet() avec une interface LignesBulletin, supprimer previewCalc de PaieDialog.tsx, corriger la faille de sécurité PUT /api/paie/[id] sans filtre company_id, corriger le bug date_fin_contrat effacé en édition, et extraire formatAnciennete / scoreLabel / scoreVariant en utilitaires partagés.

Purpose: Un changement de taux fiscal dans lib/paie-ci.ts doit se répercuter immédiatement dans la preview PaieDialog sans aucune autre modification. C'est le critère de succès SOC-02.
Output: lib/paie-ci.ts avec calculerBulletinComplet(), lib/utils-rh.ts avec scoreLabel/scoreVariant, PaieDialog sans previewCalc, PUT sécurisé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-stabilisation/01-RESEARCH.md
@.planning/phases/01-stabilisation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Interfaces existantes à lire avant d'implémenter -->
<!-- Lire lib/paie-ci.ts pour connaître la signature actuelle de calculerBulletin() -->
<!-- et toutes les constantes fiscales (SMIG_MENSUEL, TAUX_CNPS_*, etc.) -->

Signature actuelle à conserver (rétrocompatibilité) :

```typescript
// Garder cette signature existante — wrapper simplifié
function calculerBulletin(salaireBrut: number, autresRetenues?: number, avances?: number): ResultatPaie
```

Nouvelle interface à créer :

```typescript
interface LignesBulletin {
  salaire_brut: number          // salaire catégoriel de base
  sursalaire?: number           // sursalaire (non imposable CNPS si > plafond)
  prime_anciennete?: number     // calculée auto ou saisie
  prime_exceptionnelle?: number
  prime_salissure?: number
  prime_depassement?: number
  prime_fonction?: number
  prime_transport?: number      // non imposable ITS
  autres_retenues?: number
  avances?: number
}

interface ResultatPaieComplet {
  total_brut: number            // somme de toutes les lignes
  total_imposable: number       // total_brut - prime_transport
  cnps_salarie: number          // 6.3% plafonné à PLAFOND_CNPS_MENSUEL
  cmu: number                   // CMU_MENSUEL forfait
  its: number                   // ITS calculé sur base après abattement 15%
  total_retenues: number        // cnps + cmu + its + autres_retenues + avances
  salaire_net: number           // total_brut - total_retenues
}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Créer calculerBulletinComplet dans lib/paie-ci.ts + lib/utils-rh.ts</name>
  <files>
    lib/paie-ci.ts,
    lib/utils-rh.ts,
    app/(dashboard)/employes/[id]/page.tsx,
    app/(dashboard)/evaluations/page.tsx,
    app/(dashboard)/paie/[id]/print/page.tsx
  </files>
  <read_first>
    - lib/paie-ci.ts (entier — pour comprendre les constantes et calculerBulletin existant)
    - app/(dashboard)/employes/[id]/page.tsx (lignes ~39 et ~62 — formatAnciennete, scoreLabel, scoreVariant)
    - app/(dashboard)/evaluations/page.tsx (lignes ~10-23 — scoreLabel, scoreVariant dupliqués)
  </read_first>
  <behavior>
    - Test 1 : calculerBulletinComplet({ salaire_brut: 75000 }) → salaire_net > 0, cnps_salarie = Math.round(75000 * 0.063), its = 0 (SMIG sous seuil ITS)
    - Test 2 : calculerBulletinComplet({ salaire_brut: 500000, prime_transport: 30000 }) → total_imposable = 500000 (prime_transport exclue), total_brut = 530000
    - Test 3 : calculerBulletinComplet({ salaire_brut: 2000000 }) → cnps_salarie = Math.round(1647315 * 0.063) (plafonné)
    - Test 4 : formatAnciennete("2021-01-01") retourne une chaîne non vide contenant "an" ou "mois"
    - Test 5 : scoreLabel(85) retourne "Excellent" (ou équivalent — à confirmer depuis le code existant)
    - Test 6 : scoreLabel(40) retourne une valeur différente de scoreLabel(85)
  </behavior>
  <action>
  ETAPE 1 — Lire lib/paie-ci.ts en entier. Identifier :
  - Les constantes SMIG_MENSUEL, TAUX_CNPS_RETRAITE_SALARIE, PLAFOND_CNPS_MENSUEL, CMU_MENSUEL, TAUX_ABATTEMENT_ITS
  - La fonction calculerBulletin() existante et son implémentation exacte
  - La fonction calculerITS() existante

  ETAPE 2 — Ajouter dans lib/paie-ci.ts (après les déclarations existantes, sans modifier l'existant) :

  ```typescript
  export interface LignesBulletin {
    salaire_brut: number
    sursalaire?: number
    prime_anciennete?: number
    prime_exceptionnelle?: number
    prime_salissure?: number
    prime_depassement?: number
    prime_fonction?: number
    prime_transport?: number
    autres_retenues?: number
    avances?: number
  }

  export interface ResultatPaieComplet {
    total_brut: number
    total_imposable: number
    cnps_salarie: number
    cmu: number
    its: number
    total_retenues: number
    salaire_net: number
  }

  export function calculerBulletinComplet(lignes: LignesBulletin): ResultatPaieComplet {
    const primeTransport = lignes.prime_transport ?? 0
    const total_brut = (lignes.salaire_brut ?? 0)
      + (lignes.sursalaire ?? 0)
      + (lignes.prime_anciennete ?? 0)
      + (lignes.prime_exceptionnelle ?? 0)
      + (lignes.prime_salissure ?? 0)
      + (lignes.prime_depassement ?? 0)
      + (lignes.prime_fonction ?? 0)
      + primeTransport

    // Prime transport non soumise à ITS
    const total_imposable = total_brut - primeTransport

    // CNPS plafonné
    const base_cnps = Math.min(total_imposable, PLAFOND_CNPS_MENSUEL)
    const cnps_salarie = Math.round(base_cnps * TAUX_CNPS_RETRAITE_SALARIE)

    // CMU forfait
    const cmu = CMU_MENSUEL

    // ITS : abattement 15% sur base imposable après CNPS
    const base_its = total_imposable - cnps_salarie
    const base_its_apres_abattement = Math.round(base_its * (1 - TAUX_ABATTEMENT_ITS))
    const its = calculerITS(base_its_apres_abattement)

    const total_retenues = cnps_salarie + cmu + its
      + (lignes.autres_retenues ?? 0)
      + (lignes.avances ?? 0)

    const salaire_net = total_brut - total_retenues

    return { total_brut, total_imposable, cnps_salarie, cmu, its, total_retenues, salaire_net }
  }
  ```

  Vérifier que TAUX_ABATTEMENT_ITS est bien défini comme constante dans lib/paie-ci.ts (valeur : 0.15). Si elle n'est pas exportée ou n'existe pas, l'ajouter comme `export const TAUX_ABATTEMENT_ITS = 0.15`.

  ETAPE 3 — Ajouter formatAnciennete dans lib/paie-ci.ts (extraire depuis employes/[id]/page.tsx) :
  Lire la définition de formatAnciennete dans app/(dashboard)/employes/[id]/page.tsx. La copier dans lib/paie-ci.ts comme `export function formatAnciennete(dateEmbauche: string): string { ... }`.

  ETAPE 4 — Créer lib/utils-rh.ts :
  Lire les définitions de scoreLabel et scoreVariant dans app/(dashboard)/evaluations/page.tsx (ou employes/[id]/page.tsx). Créer lib/utils-rh.ts avec :

  ```typescript
  // Utilitaires partagés pour les évaluations RH
  export function scoreLabel(score: number): string { ... } // copier l'implémentation existante
  export function scoreVariant(score: number): string { ... } // copier l'implémentation existante
  ```

  ETAPE 5 — Remplacer les définitions locales dans les fichiers concernés :

- app/(dashboard)/employes/[id]/page.tsx : supprimer la définition locale de formatAnciennete, scoreLabel, scoreVariant et importer depuis lib/paie-ci.ts et lib/utils-rh.ts
- app/(dashboard)/evaluations/page.tsx : supprimer scoreLabel, scoreVariant locaux, importer depuis lib/utils-rh.ts
- app/(dashboard)/paie/[id]/print/page.tsx : supprimer la définition locale de formatAnciennete, importer depuis lib/paie-ci.ts
  </action>

  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && grep -c "calculerBulletinComplet" lib/paie-ci.ts && grep -c "LignesBulletin" lib/paie-ci.ts && grep -c "scoreLabel" lib/utils-rh.ts && grep -c "formatAnciennete" lib/paie-ci.ts && npx tsc --noEmit && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export function calculerBulletinComplet" lib/paie-ci.ts retourne 1
    - grep -c "export interface LignesBulletin" lib/paie-ci.ts retourne 1
    - grep -c "export function formatAnciennete" lib/paie-ci.ts retourne 1
    - Le fichier lib/utils-rh.ts existe et contient scoreLabel et scoreVariant exportés
    - grep "scoreLabel" app/(dashboard)/employes/[id]/page.tsx — la définition locale est supprimée, une import depuis lib/utils-rh.ts est présente
    - grep "formatAnciennete" app/(dashboard)/employes/[id]/page.tsx — la définition locale est supprimée, une import depuis lib/paie-ci.ts est présente
    - npx tsc --noEmit retourne exit code 0
  </acceptance_criteria>
  <done>calculerBulletinComplet() exportée depuis lib/paie-ci.ts. formatAnciennete exportée depuis lib/paie-ci.ts. scoreLabel/scoreVariant dans lib/utils-rh.ts. Définitions locales dupliquées supprimées.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Supprimer previewCalc de PaieDialog + corriger PUT + corriger EmployeeDialog</name>
  <files>
    components/rh/PaieDialog.tsx,
    app/api/paie/[id]/route.ts,
    components/rh/EmployeeDialog.tsx
  </files>
  <read_first>
    - components/rh/PaieDialog.tsx (entier — pour comprendre previewCalc lignes 86-115)
    - app/api/paie/[id]/route.ts (entier — pour localiser le PUT sans filtre company_id, lignes 23-44)
    - components/rh/EmployeeDialog.tsx (pour localiser l'initialisation de date_fin_contrat ligne ~122)
  </read_first>
  <action>
  ETAPE 1 — Refactorer PaieDialog.tsx : supprimer previewCalc, appeler calculerBulletinComplet.

  Lire les lignes 86-115 de PaieDialog.tsx pour comprendre exactement ce que previewCalc calcule.

  Remplacer previewCalc par un appel à calculerBulletinComplet :

  ```typescript
  import { calculerBulletinComplet, type LignesBulletin } from '@/lib/paie-ci'

  // Dans le composant, là où previewCalc est appelée, la remplacer par :
  const preview = calculerBulletinComplet({
    salaire_brut: formValues.salaire_brut ?? 0,
    sursalaire: formValues.sursalaire ?? 0,
    prime_anciennete: formValues.prime_anciennete ?? 0,
    prime_exceptionnelle: formValues.prime_exceptionnelle ?? 0,
    prime_salissure: formValues.prime_salissure ?? 0,
    prime_depassement: formValues.prime_depassement ?? 0,
    prime_fonction: formValues.prime_fonction ?? 0,
    prime_transport: formValues.prime_transport ?? 0,
    autres_retenues: formValues.autres_retenues ?? 0,
    avances: formValues.avances ?? 0,
  })
  ```

  Mettre à jour l'affichage de la preview pour utiliser preview.salaire_net, preview.cnps_salarie, preview.its, preview.cmu à la place des valeurs calculées localement par previewCalc.

  Supprimer entièrement la fonction previewCalc du fichier.

  ETAPE 2 — Corriger app/api/paie/[id]/route.ts : ajouter le filtre company_id dans PUT.

  Localiser le handler PUT (lignes 23-44 d'après CONCERNS.md). Actuellement :

  ```typescript
  .update({ statut: parsed.data.statut })
  .eq("id", params.id)
  ```

  Corriger en ajoutant le filtrage multi-tenant :

  ```typescript
  const { data: companyId } = await supabase.rpc('get_user_company_id')
  if (!companyId) {
    return NextResponse.json({ error: "Entreprise introuvable" }, { status: 403 })
  }

  const { data, error } = await supabase
    .from("bulletins_paie")
    .update({ statut: parsed.data.statut })
    .eq("id", params.id)
    .eq("company_id", companyId as string)
  ```

  ETAPE 3 — Corriger EmployeeDialog.tsx : date_fin_contrat initialisé avec la valeur existante.

  Lire la ligne ~122 de EmployeeDialog.tsx. Actuellement :

  ```typescript
  date_fin_contrat: "",  // Bug : efface la valeur existante en mode édition
  ```

  Corriger en utilisant la valeur de l'employé existant :

  ```typescript
  // Dans les defaultValues du formulaire (mode édition)
  date_fin_contrat: employee?.date_fin_contrat ?? "",
  // ou si l'accès aux defaultValues se fait différemment :
  date_fin_contrat: initialData?.date_fin_contrat ?? "",
  ```

  La valeur exacte dépend de comment les props sont nommées dans le composant — lire le fichier avant de coder.
  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && grep -c "previewCalc" components/rh/PaieDialog.tsx | grep -q "^0$" && grep -c "calculerBulletinComplet" components/rh/PaieDialog.tsx && grep -c "company_id" app/api/paie/\[id\]/route.ts && npx tsc --noEmit && echo "OK: previewCalc supprimé, PUT sécurisé, TypeScript propre"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "previewCalc" components/rh/PaieDialog.tsx retourne 0 (aucune occurrence)
    - grep -c "calculerBulletinComplet" components/rh/PaieDialog.tsx retourne > 0
    - grep -c "from '@/lib/paie-ci'" components/rh/PaieDialog.tsx retourne > 0 (ou import équivalent)
    - grep -n "get_user_company_id" app/api/paie/[id]/route.ts montre que le PUT utilise maintenant company_id
    - grep "date_fin_contrat" components/rh/EmployeeDialog.tsx — la valeur n'est plus initialisée à "" inconditionnellement
    - npx tsc --noEmit retourne exit code 0
  </acceptance_criteria>
  <done>PaieDialog.tsx utilise calculerBulletinComplet() depuis lib/paie-ci.ts. PUT /api/paie/[id] filtre par company_id. EmployeeDialog.tsx initialise date_fin_contrat depuis les données de l'employé en mode édition.</done>
</task>

</tasks>

<verification>
Après les deux tâches :
1. grep -rn "previewCalc" components/ — doit retourner 0 résultat
2. grep -c "calculerBulletinComplet" components/rh/PaieDialog.tsx — doit retourner > 0
3. grep -n "company_id" app/api/paie/[id]/route.ts — doit montrer le filtre dans le PUT
4. npx tsc --noEmit — doit passer sans erreur
5. Test SOC-02 : modifier TAUX_CNPS_RETRAITE_SALARIE dans lib/paie-ci.ts de 6.3% à 6.4% → la preview PaieDialog doit refléter immédiatement le changement sans autre modification. (Remettre 6.3% après le test.)
</verification>

<success_criteria>

- previewCalc n'existe plus dans PaieDialog.tsx
- PaieDialog.tsx importe et utilise calculerBulletinComplet depuis lib/paie-ci.ts
- PUT /api/paie/[id] filtre par company_id
- date_fin_contrat correctement initialisé en mode édition
- formatAnciennete, scoreLabel, scoreVariant en utilitaires partagés (plus de duplication)
- npx tsc --noEmit passe
- SOC-02 satisfait : single source of truth pour le calcul de paie
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-stabilisation/01-03-SUMMARY.md` avec :
- Interface LignesBulletin (signature complète)
- Fichiers modifiés et nature des changements
- Résultat npx tsc --noEmit
- Confirmation de la suppression de previewCalc
</output>
