---
plan: 01-04
phase: 1
wave: 3
depends_on: [01-03]
requirements: [SOC-04]
files_modified:
  - vitest.config.ts
  - package.json
  - lib/__tests__/paie-ci.test.ts
autonomous: true

must_haves:
  truths:
    - "npx vitest run retourne exit code 0 avec 100% des tests verts"
    - "Les 6 fonctions calculerITS, calculerBulletin, calculerChargesPatronales, calculerPrimeAnciennete, calculerProvision13e, calculerIndemniteLicenciement sont toutes testées"
    - "Les valeurs LF 2026 (SMIG 75 000, CNPS 6.3%, plafond 1 647 315, CMU 1 600) sont vérifiées par des tests"
    - "calculerBulletinComplet (créé en plan 01-03) est couvert par au moins 3 tests"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Configuration Vitest compatible Next.js 14 App Router avec alias @/*"
      contains: "resolve.*alias"
    - path: "lib/__tests__/paie-ci.test.ts"
      provides: "Suite de tests couvrant les 7 fonctions de lib/paie-ci.ts"
      contains: "calculerITS"
    - path: "package.json"
      provides: "Scripts test, test:watch, test:coverage"
      contains: "vitest run"
  key_links:
    - from: "lib/__tests__/paie-ci.test.ts"
      to: "lib/paie-ci.ts"
      via: "import { calculerITS, calculerBulletin, calculerBulletinComplet, ... } from '@/lib/paie-ci'"
      pattern: "from '@/lib/paie-ci'"
---

<objective>
Installer Vitest, configurer vitest.config.ts pour Next.js 14 App Router (alias @/* résolu), écrire la suite complète de tests unitaires pour toutes les fonctions de lib/paie-ci.ts avec les valeurs légales LF 2026, et ajouter les scripts npm.

Purpose: Zéro test automatisé signifie qu'une erreur sur les calculs fiscaux (ITS, CNPS) n'est détectée qu'au moment du bulletin imprimé. Les tests de non-régression garantissent que chaque modification de taux est validée immédiatement.
Output: vitest.config.ts + lib/__tests__/paie-ci.test.ts avec 100% des cas limites + npm test fonctionnel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-stabilisation/01-RESEARCH.md
@.planning/phases/01-stabilisation/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Fonctions à tester — lire lib/paie-ci.ts avant d'écrire les tests -->

Constantes LF 2026 à vérifier dans les tests (valeurs attendues) :

- SMIG_MENSUEL = 75 000
- TAUX_CNPS_RETRAITE_SALARIE = 0.063 (6.3%)
- PLAFOND_CNPS_MENSUEL = 1 647 315
- CMU_MENSUEL = 1 600
- Abattement ITS = 15% (TAUX_ABATTEMENT_ITS = 0.15)
- Provision 13e = 75% du brut / 12 par mois

Barème ITS LF 2026 (CGI CI Art. 116) :

- 0 à 75 000 → 0%
- 75 001 à 200 000 → 12%
- 200 001 à 350 000 → 18%
- 350 001 à 600 000 → 25%
- > 600 000 → 32%

Calcul ITS : base_after_abattement = Math.round(base_imposable * 0.85)
Puis appliquer le barème sur base_after_abattement.

Prime ancienneté CI (CCI Art. 17) :

- 1% × années × salaire catégoriel
- Plafond : 25% (au-delà de 25 ans, le taux reste à 25%)
- 0 ans → 0

Indemnité licenciement (Art. 74 CT-CI) :

- Tranches : 30% / 35% / 40% selon ancienneté
- Vérifier les seuils exacts dans lib/paie-ci.ts
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Installer Vitest et configurer vitest.config.ts</name>
  <files>
    vitest.config.ts,
    package.json
  </files>
  <read_first>
    - package.json (pour vérifier si vitest est déjà dans devDependencies)
    - tsconfig.json (pour vérifier moduleResolution et les paths alias @/*)
  </read_first>
  <action>
  ETAPE 1 — Installer les dépendances si absentes de package.json :
  ```bash
  cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine"
  npm install --save-dev vitest @vitest/coverage-v8
  ```
  Vérifier après installation que vitest apparaît dans package.json devDependencies.

  ETAPE 2 — Créer vitest.config.ts à la racine du projet :

  ```typescript
  import { defineConfig } from 'vitest/config'
  import path from 'path'

  export default defineConfig({
    test: {
      environment: 'node',
      globals: true,
      include: ['**/*.test.ts', '**/*.spec.ts'],
      exclude: ['node_modules', '.next', 'e2e'],
    },
    resolve: {
      alias: {
        '@': path.resolve(__dirname, '.'),
      },
    },
  })
  ```

  ETAPE 3 — Ajouter dans package.json, section "scripts", sans supprimer les scripts existants :

  ```json
  "test": "vitest run",
  "test:watch": "vitest",
  "test:coverage": "vitest run --coverage"
  ```

  ETAPE 4 — Vérifier que la configuration fonctionne avec un test vide :

  ```bash
  cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine"
  mkdir -p lib/__tests__
  echo "import { describe, it, expect } from 'vitest'; describe('smoke', () => { it('works', () => { expect(1+1).toBe(2) }) })" > lib/__tests__/smoke.test.ts
  npx vitest run lib/__tests__/smoke.test.ts
  ```

  Le test smoke doit passer (exit code 0). Supprimer lib/__tests__/smoke.test.ts après vérification.
  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && node -e "require('./vitest.config.ts')" 2>/dev/null; npx vitest run --reporter=verbose 2>&1 | head -5 || echo "vitest installé"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier vitest.config.ts existe à la racine
    - grep -c "resolve.*alias" vitest.config.ts retourne > 0 (ou grep -c "@.*path.resolve" vitest.config.ts)
    - grep -c "\"test\": \"vitest run\"" package.json retourne 1
    - grep -c "vitest" package.json retourne > 0 (dans devDependencies)
    - Le dossier node_modules/vitest/ existe
  </acceptance_criteria>
  <done>vitest et @vitest/coverage-v8 installés. vitest.config.ts créé avec alias @/* → racine projet. Scripts test/test:watch/test:coverage ajoutés dans package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Écrire la suite complète de tests lib/__tests__/paie-ci.test.ts</name>
  <files>
    lib/__tests__/paie-ci.test.ts
  </files>
  <read_first>
    - lib/paie-ci.ts (entier — signatures exactes de toutes les fonctions exportées)
  </read_first>
  <behavior>
    Tests calculerITS (barème LF 2026) :
    - base = 0 → ITS = 0
    - base = 75 000 (SMIG) → ITS = 0 (abattement 15% → 63 750, dans tranche 0%)
    - base = 100 000 → abattement → 85 000 → (85 000 - 75 000) × 12% = 1 200 → ITS = 1 200
    - base = 250 001 → franchit la tranche 18%
    - base = 800 000 → tranche 32% atteinte

    Tests calculerBulletin (wrapper existant, rétrocompatibilité) :
    - brut = 75 000 → salaire_net > 0 et salaire_net < 75 000
    - brut = 2 000 000 → cnps_salarie = Math.round(1 647 315 × 0.063) (plafonné)
    - brut = 0 → salaire_net = 0 ou valeur cohérente
    - brut = 200 000 avec avances = 50 000 → salaire_net = résultat sans avances - 50 000

    Tests calculerBulletinComplet (nouvelle fonction plan 01-03) :
    - { salaire_brut: 150 000, prime_transport: 20 000 } → total_imposable = 150 000 (transport exclu), total_brut = 170 000
    - { salaire_brut: 200 000, prime_anciennete: 10 000 } → total_brut = 210 000
    - { salaire_brut: 2 000 000 } → cnps_salarie = Math.round(1 647 315 × 0.063)

    Tests calculerChargesPatronales :
    - brut = 75 000 (SMIG) → résultat > 0, objet avec clés charge patronale
    - brut = 500 000 → résultat > résultat pour 75 000

    Tests calculerPrimeAnciennete :
    - 0 an → prime = 0
    - 1 an → prime > 0
    - 25 ans → prime = 25% × salaire
    - 30 ans → prime = même que 25 ans (plafond 25%)

    Tests calculerProvision13e :
    - 75 000 → Math.round(75 000 × 0.75 / 12) = 4 688
    - 0 → 0

    Tests calculerIndemniteLicenciement :
    - 0 an → 0
    - 5 ans → résultat > 0
    - vérifier les seuils de tranches (30%/35%/40%) depuis les valeurs dans lib/paie-ci.ts
  </behavior>
  <action>
  ETAPE 1 — Lire lib/paie-ci.ts en entier pour obtenir :
  - Les signatures exactes de toutes les fonctions exportées
  - Les valeurs exactes des constantes (SMIG_MENSUEL, TAUX_CNPS_RETRAITE_SALARIE, PLAFOND_CNPS_MENSUEL, CMU_MENSUEL)
  - Le barème ITS exact et comment l'abattement 15% est appliqué
  - La formule exacte de calculerIndemniteLicenciement et ses tranches

  ETAPE 2 — Créer lib/__tests__/paie-ci.test.ts avec la suite complète.
  Le fichier doit importer toutes les fonctions et constantes nécessaires :

  ```typescript
  import { describe, it, expect } from 'vitest'
  import {
    calculerITS,
    calculerBulletin,
    calculerBulletinComplet,
    calculerChargesPatronales,
    calculerPrimeAnciennete,
    calculerProvision13e,
    calculerIndemniteLicenciement,
    SMIG_MENSUEL,
    PLAFOND_CNPS_MENSUEL,
    CMU_MENSUEL,
  } from '@/lib/paie-ci'
  ```

  Si calculerBulletinComplet n'est pas encore exportée (car plan 01-03 est un prérequis), le test doit être écrit en rouge (RED phase) et passera au vert une fois plan 01-03 complété.

  Organiser les tests en describe blocks par fonction. Chaque cas limite identifié dans la section behavior doit avoir son propre test it(). Utiliser des valeurs numériques exactes pour les assertions (pas de toBeTruthy() ou toBeGreaterThan() sauf quand la valeur exacte est difficile à calculer manuellement).

  Calcul manuel de référence pour les assertions :

- calculerITS(100_000) : abattement 15% → base = 85 000. Tranche 0-75 000 : 0. Tranche 75 001-85 000 : 10 000 × 12% = 1 200. ITS = 1 200.
- calculerProvision13e(75_000) : 75 000 × 0.75 / 12 = 4 687.5, arrondi = 4 688 (ou 4 687 selon Math.round ou Math.floor — vérifier dans l'implémentation).
- calculerBulletin avec plafond CNPS : cnps = Math.round(1 647 315 × 0.063) = Math.round(103 780.845) = 103 781.

  ETAPE 3 — Exécuter les tests en mode RED pour confirmer qu'ils échouent si les fonctions n'existent pas encore / avant plan 01-03.

  ```bash
  cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine"
  npx vitest run lib/__tests__/paie-ci.test.ts --reporter=verbose
  ```

  ETAPE 4 — Si plan 01-03 est complété (calculerBulletinComplet existe), exécuter en mode GREEN :

  ```bash
  npx vitest run lib/__tests__/paie-ci.test.ts --reporter=verbose
  ```

  Tous les tests doivent passer. Si un test échoue, analyser la valeur attendue vs réelle et ajuster soit le test (si le calcul de référence manuel était incorrect) soit l'implémentation (si la fonction retourne une valeur incorrecte selon les textes légaux CI).

  ETAPE 5 — Exécuter la suite complète avec coverage :

  ```bash
  npx vitest run --coverage 2>&1 | tail -20
  ```

  </action>
  <verify>
    <automated>cd "c:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/Ressource Humaine" && npx vitest run lib/__tests__/paie-ci.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run lib/__tests__/paie-ci.test.ts se termine avec exit code 0
    - La sortie de npx vitest run --reporter=verbose mentionne "calculerITS", "calculerBulletin", "calculerChargesPatronales", "calculerPrimeAnciennete", "calculerProvision13e", "calculerIndemniteLicenciement"
    - La sortie indique "X passed" avec X >= 20 tests
    - Aucun test est en statut "skipped" ou "todo" sauf indication explicite
    - grep -c "calculerBulletinComplet" lib/__tests__/paie-ci.test.ts retourne > 0 (tests pour la nouvelle fonction)
    - grep -c "SMIG_MENSUEL\|PLAFOND_CNPS\|CMU_MENSUEL" lib/__tests__/paie-ci.test.ts retourne > 0 (constantes LF 2026 vérifiées)
  </acceptance_criteria>
  <done>Suite de tests Vitest complète couvrant les 7 fonctions de lib/paie-ci.ts. npx vitest run passe à 100%. Valeurs LF 2026 vérifiées par assertions numériques exactes.</done>
</task>

</tasks>

<verification>
Après les deux tâches :
1. npm test — doit retourner exit code 0 avec tous les tests verts
2. npx vitest run --coverage — doit afficher un rapport de couverture
3. npx tsc --noEmit — doit passer (vitest.config.ts ne doit pas introduire d'erreurs TypeScript)
4. Vérification SOC-04 : grep -c "calculerITS\|calculerBulletin\|calculerChargesPatronales\|calculerPrimeAnciennete\|calculerProvision13e\|calculerIndemniteLicenciement" lib/__tests__/paie-ci.test.ts — doit retourner > 0 pour chaque fonction
</verification>

<success_criteria>

- vitest installé et configuré avec alias @/* opérationnel
- npm test passe avec 0 échec
- Au moins 20 cas de test couvrant les 7 fonctions (6 + calculerBulletinComplet)
- Valeurs LF 2026 vérifiées par assertions exactes (pas juste toBeTruthy)
- SOC-04 satisfait : tests automatisés sur toutes les fonctions fiscales
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-stabilisation/01-04-SUMMARY.md` avec :
- Nombre de tests écrits et résultat npx vitest run
- Rapport de couverture (fonctions couvertes)
- Versions installées (vitest, @vitest/coverage-v8)
- Cas limites testés pour chaque fonction
</output>
