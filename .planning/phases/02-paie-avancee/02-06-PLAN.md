---
description: "Intégration Convention Collective dans les calculs de paie"
labels: ["backend"]
wave: 4
autonomous: true
depends_on:
  - "02-05-PLAN.md"
files_modified:
  - "lib/paie-ci.ts"
  - "lib/__tests__/paie-ci.test.ts"
requirements_addressed:
  - "PAI-06"
---

# Plan: 02-06 Intégration Convention

<objective>
Utiliser les paramètres fiscaux (convention collective, valeur du point) pour automatiser le calcul des primes d'ancienneté et gérer les spécificités des secteurs.
</objective>

<tasks>

<task>
<description>Mettre à jour `calculerPrimeAnciennete` pour supporter les spécificités par convention.</description>
<read_first>
- lib/paie-ci.ts
</read_first>
<action>
1. Modifier la signature de `calculerPrimeAnciennete` pour accepter un argument optionnel `convention: string = 'Interprofessionnelle'`.
2. Implémenter des règles spécifiques. Par exemple :
   - 'Interprofessionnelle' (défaut) : plafond 25%, 1% par année.
   - 'BTP' : plafond potentiellement différent (ex: 20%).
3. Mettre à jour les tests dans `lib/__tests__/paie-ci.test.ts`.
</action>
<acceptance_criteria>
- Les calculs de prime varient selon la convention indiquée.
- Les tests existants ne cassent pas (fallback par défaut respecté).
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Lancer les tests unitaires via `npm run test` pour lib/paie-ci.ts.
</verification>

<must_haves>

- L'algorithme de paie-ci.ts doit utiliser les conventions définies dans la base.
</must_haves>
