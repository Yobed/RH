---
phase: 04-dossier-personnel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/employes/[id]/page.tsx
  - components/rh/GedTabClient.tsx
autonomous: true
requirements:
  - DOS-01

must_haves:
  truths:
    - "L'onglet GED de la fiche employé affiche un filtre par famille qui réduit les documents affichés en temps réel"
    - "Tous les documents d'un employé sont listés sans limite (toute la liste est visible)"
    - "Chaque document affiche un badge coloré selon sa famille (Contrat=bleu, Médical=rouge, Paie=vert, etc.)"
  artifacts:
    - path: "components/rh/GedTabClient.tsx"
      provides: "Client component — filtrage en mémoire des documents de l'onglet GED"
      exports: ["GedTabClient"]
    - path: "app/(dashboard)/employes/[id]/page.tsx"
      provides: "Passe documents + employeeId à GedTabClient dans l'onglet GED"
  key_links:
    - from: "app/(dashboard)/employes/[id]/page.tsx"
      to: "components/rh/GedTabClient.tsx"
      via: "props initialDocuments"
      pattern: "GedTabClient"
    - from: "components/rh/GedTabClient.tsx"
      to: "documents"
      via: "filtre en mémoire par famille"
      pattern: "selectedFamille"
---

<objective>
Améliorer le module GED (onglet "GED") de la fiche employé en ajoutant un filtre par famille en mémoire et des badges colorés par catégorie de document.

Purpose: La page `/archives` globale est complète (pagination, recherche, filtre). L'onglet GED de la fiche employé affiche une liste brute sans aucun filtrage — DOS-01 exige que les documents soient classés et filtrables par famille.

Output: Un composant client `GedTabClient.tsx` gérant le filtre + badges, intégré dans l'onglet GED de `app/(dashboard)/employes/[id]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-dossier-personnel/04-RESEARCH.md

<interfaces>
<!-- Contrat de données existant — l'executor NE DOIT PAS explorer la codebase -->

Depuis app/(dashboard)/employes/[id]/page.tsx (lignes 125, 519-606):
```typescript
// Données documents chargées côté serveur
const { data: documents } = await supabase
  .from("documents")
  .select("id, name, famille, file_type, file_size_kb, created_at, file_url")
  .eq("employee_id", params.id)
  .order("created_at", { ascending: false });
// PAS de .limit() — toute la liste

// Onglet GED actuel (Server Component, lignes 519-606):
// Affiche documents.map() sans filtre. A remplacer par <GedTabClient />.
```

Type Document dans l'onglet GED (inféré depuis le select):
```typescript
type DocumentRow = {
  id: string;
  name: string;
  famille: string | null;
  file_type: string | null;
  file_size_kb: number | null;
  created_at: string | null;
  file_url: string;
};
```

Familles disponibles dans DocumentUploadDialog (14 valeurs):
```
"Contrat" | "Avenant" | "Diplomes" | "CNI / Passeport" | "Extrait de naissance"
| "Casier judiciaire" | "CV" | "Paie" | "Medical" | "Conges"
| "Disciplinaire" | "Demande d'explication" | "Formation" | "Autre"
```

Composants deja utilises dans l'onglet GED:
```typescript
import { DocumentUploadDialog } from "@/components/rh/DocumentUploadDialog";
// Bouton d'upload existant — a conserver dans GedTabClient
```

Section Conformite Legale (lignes 528-551) — a integrer dans GedTabClient:
```typescript
const REQUIS = ["CNI / Passeport", "Extrait de naissance", "Contrat", "CV", "Diplomes"];
// Affiche badge vert (present) ou rouge (absent) pour chaque famille requise
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer GedTabClient — filtre famille + badges colores</name>
  <files>components/rh/GedTabClient.tsx</files>
  <read_first>
    - app/(dashboard)/employes/[id]/page.tsx (lignes 519-606 — onglet GED actuel a reproduire + ameliorer)
    - components/rh/DocumentUploadDialog.tsx (pour importer et conserver le bouton upload)
  </read_first>
  <action>
Creer `components/rh/GedTabClient.tsx` — "use client".

Props interface:
```typescript
interface GedTabClientProps {
  initialDocuments: DocumentRow[];
  employeeId: string;
  companyId: string;
}
type DocumentRow = {
  id: string;
  name: string;
  famille: string | null;
  file_type: string | null;
  file_size_kb: number | null;
  created_at: string | null;
  file_url: string;
};
```

State: `const [selectedFamille, setSelectedFamille] = useState<string>("Tous")`.

Filtrage en memoire:
```typescript
const filtered = selectedFamille === "Tous"
  ? initialDocuments
  : initialDocuments.filter((d) => d.famille === selectedFamille);
```

Familles pour le filtre — uniquement les familles presentes dans les documents:
```typescript
const familles = ["Tous", ...Array.from(new Set(initialDocuments.map(d => d.famille).filter((f): f is string => f !== null)))];
```

UI du filtre: rangee de boutons pill horizontaux scrollables (`flex gap-2 overflow-x-auto pb-1 flex-wrap`). Bouton actif: `bg-[oklch(0.175_0.04_248)] text-white rounded-full px-3 py-1 text-xs font-semibold`. Boutons inactifs: `bg-slate-100 text-slate-600 hover:bg-slate-200 rounded-full px-3 py-1 text-xs font-semibold`.

Couleurs badge famille (span `rounded-lg px-2 py-0.5 text-[11px] font-semibold`):
- "Contrat" => `bg-blue-50 text-blue-700`
- "Avenant" => `bg-amber-50 text-amber-700`
- "Paie" => `bg-emerald-50 text-emerald-700`
- "Medical" => `bg-red-50 text-red-700`
- "Disciplinaire" => `bg-orange-50 text-orange-700`
- "Formation" => `bg-purple-50 text-purple-700`
- "CNI / Passeport" | "Extrait de naissance" | "Casier judiciaire" => `bg-sky-50 text-sky-700`
- null ou autre => `bg-slate-100 text-slate-600`

Implementer une fonction helper:
```typescript
function getFamilleColor(famille: string | null): string {
  // retourne la classe tailwind selon le mapping ci-dessus
}
```

Layout: grille `grid gap-6 lg:grid-cols-4` avec:
- Colonne gauche (lg:col-span-1): Section "Conformite Legale" — reproduire les 5 items REQUIS depuis la version Server Component actuelle
- Colonne droite (lg:col-span-3): header avec titre + compteur + DocumentUploadDialog, puis rangee de filtres pill, puis tableau

Tableau des documents (colonnes: Nom, Categorie, Taille, Date, Telecharger):
- Taille: `${doc.file_size_kb} Ko` si non null sinon "—"
- Date: `new Date(doc.created_at).toLocaleDateString("fr-CI")` avec fallback "—"
- Telecharger: `<a href={doc.file_url} target="_blank" rel="noopener noreferrer" className="text-[oklch(0.175_0.04_248)] hover:underline font-semibold text-xs">Telecharger</a>`

Etat vide: `<p className="p-10 text-center text-sm text-slate-600 italic">Aucun document dans cette categorie.</p>`

NO `any` — utiliser `DocumentRow` partout. No `console.log`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "GedTabClient|error" | head -20 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectedFamille" components/rh/GedTabClient.tsx` retourne >= 2
    - `grep "use client" components/rh/GedTabClient.tsx` retourne 1 ligne
    - `grep "DocumentUploadDialog" components/rh/GedTabClient.tsx` retourne 1 ligne
    - `grep ": any" components/rh/GedTabClient.tsx` retourne 0 ligne
    - Le fichier existe: `ls components/rh/GedTabClient.tsx`
  </acceptance_criteria>
  <done>GedTabClient exporte avec filtre famille en memoire, badges colores, section conformite legale et bouton upload conserve.</done>
</task>

<task type="auto">
  <name>Task 2: Integrer GedTabClient dans l'onglet GED de la fiche employe</name>
  <files>app/(dashboard)/employes/[id]/page.tsx</files>
  <read_first>
    - app/(dashboard)/employes/[id]/page.tsx (entier — pour localiser l'onglet GED lignes 519-606 et la section des imports)
    - components/rh/GedTabClient.tsx (cree en Task 1 — pour connaitre les props exactes)
  </read_first>
  <action>
Dans `app/(dashboard)/employes/[id]/page.tsx`:

1. Ajouter l'import (apres les imports existants de composants rh):
   ```typescript
   import { GedTabClient } from "@/components/rh/GedTabClient";
   ```

2. Verifier que le select `documents` ligne 125 ne contient pas `.limit()` — si present, le supprimer.

3. Remplacer tout le contenu de `<TabsContent value="ged">` (lignes 520-606 environ) par:
   ```tsx
   <TabsContent value="ged" className="space-y-6 pt-2">
     <GedTabClient
       initialDocuments={documents ?? []}
       employeeId={emp.id}
       companyId={emp.company_id}
     />
   </TabsContent>
   ```

4. Verifier si `DocumentUploadDialog` est encore utilise ailleurs dans la page. S'il n'est utilise QUE dans l'ancien onglet GED, supprimer son import du fichier parent (il est maintenant dans GedTabClient). S'il est utilise ailleurs, conserver l'import.

5. Supprimer egalement l'import `ShieldCheck` de @phosphor-icons/react si plus utilise apres ce refactoring.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "employes|error TS" | head -20 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "GedTabClient" "app/(dashboard)/employes/[id]/page.tsx"` retourne >= 2 lignes (import + usage JSX)
    - `grep "initialDocuments" "app/(dashboard)/employes/[id]/page.tsx"` retourne 1 ligne
    - `npx tsc --noEmit` termine sans erreur TS
  </acceptance_criteria>
  <done>L'onglet GED de la fiche employe delegue tout le rendu a GedTabClient avec filtre famille actif.</done>
</task>

</tasks>

<verification>
```bash
# 1. TypeScript compile proprement
npx tsc --noEmit

# 2. Filtre en memoire present
grep -c "selectedFamille" components/rh/GedTabClient.tsx

# 3. Integration dans la fiche
grep "GedTabClient" "app/(dashboard)/employes/[id]/page.tsx"

# 4. Pas de any
grep ": any" components/rh/GedTabClient.tsx && echo "FAIL — any detecte" || echo "OK"

# 5. Pas de console.log
grep "console.log" components/rh/GedTabClient.tsx && echo "FAIL" || echo "OK"
```
</verification>

<success_criteria>
- Onglet GED de la fiche employe: filtre par famille fonctionnel (changement de famille met a jour la liste immediatement)
- Badges colores par famille presents sur chaque ligne de document
- Section Conformite Legale (5 documents requis) integree dans GedTabClient
- TypeScript compile sans erreur
- Aucun `any`, aucun `console.log`
</success_criteria>

<output>
Apres execution, creer `.planning/phases/04-dossier-personnel/04-01-SUMMARY.md`
</output>
