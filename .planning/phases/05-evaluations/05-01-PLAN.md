---
phase: 05-evaluations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260402000000_evaluations.sql
  - types/supabase.ts
  - app/(dashboard)/evaluations/page.tsx
  - components/rh/EvaluationsList.tsx
autonomous: true
requirements:
  - EVA-01
  - EVA-02

must_haves:
  truths:
    - "L'entreprise peut planifier des évaluations pour un employé (annuelle, semestrielle, période d'essai)."
    - "Chaque évaluation dispose d'un statut (PLANIFIEE, EN_COURS, TERMINEE, ANNULEE)."
    - "La base de données gère les évaluations avec RLS."
  artifacts:
    - path: "supabase/migrations/20260402000000_evaluations.sql"
      provides: "Tables 'evaluations' avec RLS."
    - path: "app/(dashboard)/evaluations/page.tsx"
      provides: "Page listant les évaluations."
  key_links:
    - from: "app/(dashboard)/evaluations/page.tsx"
      to: "evaluations"
      via: "Supabase SSR queries"
---

<objective>
Poser la fondation du module Évaluations : création des tables `evaluations` en base de données, mise à jour des types TypeScript, et création de la page de tableau de bord principale pour les évaluations afin de lister les données existantes.

Purpose: Sans structure de données, on ne peut pas créer ni soumettre de formulaires d'évaluation.
Output: Migration SQL, types Supabase à jour, Page Evaluations (liste vide pour l'instant avec layout structuré).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Future Table evaluations (types/supabase.ts) -->
```typescript
{
  id: string
  company_id: string
  employee_id: string
  evaluateur_id: string | null
  titre: string
  type: string         // "ANNUELLE" | "SEMESTRIELLE" | "TRIMESTRIELLE" | "PERIODE_ESSAI" | "AUTRE"
  statut: string       // "PLANIFIEE" | "EN_COURS" | "TERMINEE" | "ANNULEE"
  date_prevue: string
  date_realisation: string | null
  score_global: number | null
  commentaires_evaluateur: string | null
  commentaires_employe: string | null
  objectifs_futurs: string | null
  criteres_evaluation: Json | null // Pour stocker les scores détaillés structurés [{critere, note, commentaire}]
  created_at: string
  updated_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Migration SQL evaluations</name>
  <files>supabase/migrations/20260402000000_evaluations.sql</files>
  <action>
    1. Créer le fichier `supabase/migrations/20260402000000_evaluations.sql`
    2. Définir la table `evaluations` avec les colonnes décrites dans les interfaces.
    3. Ajouter la Foreign Key `company_id` REFERENCES `companies(id) ON DELETE CASCADE`.
    4. Ajouter la Foreign Key `employee_id` REFERENCES `employees(id) ON DELETE CASCADE`.
    5. Ajouter la RLS policy : isolation par `company_id = get_user_company_id()`.
  </action>
  <verify>
    <automated>psql, ou exécution de la migration en base locale / projet test.</automated>
  </verify>
  <done>La migration SQL est créée, vérifiée, et les index sur (company_id, employee_id) sont ajoutés.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Mise à jour types TypeScript supabase.ts</name>
  <files>types/supabase.ts</files>
  <action>
    1. Dans `types/supabase.ts`, ajouter l'interface pour `evaluations` dans `Database['public']['Tables']`.
    2. S'assurer de respecter les nullabilités (par ex. `score_global` nullable, `statut` non nullable avec default, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Zero erreurs de types.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 : Création Page Evaluations</name>
  <files>app/(dashboard)/evaluations/page.tsx, components/rh/EvaluationsList.tsx</files>
  <action>
    1. Créer `app/(dashboard)/evaluations/page.tsx` comme Server Component. Récupérer toutes les évaluations de la compagnie.
    2. Créer `components/rh/EvaluationsList.tsx` (Client Component) : afficher un DataTable simple ou une grille montrant les évaluations (employé, type, date_prevue, statut).
    3. Ajouter l'icône / le lien `Évaluations` dans `components/layout/Sidebar.tsx` s'il n'y est pas (icône `ClipboardCheck` ou similaire).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>La page compile et se lance correctement. Le menu dispose bien de l'onglet Évaluations.</done>
</task>

</tasks>

<verification>
1. La page `/evaluations` est accessible sans crash via le menu latéral.
2. `npx tsc --noEmit` passe sans erreur.
</verification>

<success_criteria>
- Table `evaluations` créée avec RLS strict.
- La liste des évaluations récupérée depuis Supabase s'affiche sur la page `/evaluations`.
- Zéro `any`.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-evaluations/05-01-SUMMARY.md`.
</output>
