---
phase: 08-import-sage-paie
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260427000000_payroll_logs.sql
  - lib/paie-sage-import.ts
  - lib/__tests__/paie-sage-import.test.ts
  - types/supabase.ts
autonomous: true
requirements:
  - ISP-03
  - ISP-04
  - ISP-05

must_haves:
  truths:
    - "La table payroll_logs existe avec toutes les 22 colonnes du mapping SagePayrollImportService + employee_name + company_id + métadonnées d'import"
    - "RLS activé sur payroll_logs avec politique isolation_company basée sur get_user_company_id()"
    - "cleanCurrency() retourne 0.0 pour les valeurs invalides/vides, et un number valide pour les formats Sage (espaces insécables, virgule décimale)"
    - "COLUMN_MAPPING exporte les 22 entrées exactes du SagePayrollImportService Python"
    - "extractEmployeeInfo() retourne { employee_id: premier_mot, employee_name: reste } depuis la valeur brute Sage"
    - "La suite de tests Vitest passe à 100% sur cleanCurrency, extractEmployeeInfo et validateRequiredColumns"
  artifacts:
    - path: "supabase/migrations/20260427000000_payroll_logs.sql"
      provides: "DDL complet de payroll_logs avec 22 colonnes + RLS"
      contains: "CREATE TABLE payroll_logs"
    - path: "lib/paie-sage-import.ts"
      provides: "SagePayrollImportService TypeScript — cleanCurrency, COLUMN_MAPPING, extractEmployeeInfo, validateRequiredColumns"
      exports: ["cleanCurrency", "COLUMN_MAPPING", "extractEmployeeInfo", "validateRequiredColumns", "REQUIRED_COLUMNS"]
    - path: "types/supabase.ts"
      provides: "Type TypeScript PayrollLog avec toutes les colonnes"
      contains: "payroll_logs"
  key_links:
    - from: "app/api/paie/import-sage/route.ts"
      to: "supabase payroll_logs"
      via: "supabase.from('payroll_logs').insert()"
      pattern: "payroll_logs"
    - from: "lib/__tests__/paie-sage-import.test.ts"
      to: "lib/paie-sage-import.ts"
      via: "import { cleanCurrency, extractEmployeeInfo, validateRequiredColumns }"
      pattern: "cleanCurrency"
---

<objective>
Créer les fondations de l'import Sage : migration SQL de la table payroll_logs avec les 22 colonnes complètes du SagePayrollImportService, mise à jour des types TypeScript, et suite de tests TDD pour les fonctions utilitaires de nettoyage et d'extraction.

Purpose: Fournir la table de destination avec toutes les colonnes du Livre de Paie Sage et garantir que la logique de nettoyage (cleanCurrency), d'extraction (extractEmployeeInfo) et de validation est correcte avant toute implémentation d'API.
Output: Migration SQL exécutable, lib/paie-sage-import.ts (équivalent TypeScript de SagePayrollImportService), types Supabase à jour, tests unitaires verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-import-sage-paie/08-CONTEXT.md
@.planning/phases/08-import-sage-paie/08-RESEARCH.md
@skills/database/SKILL.md
@skills/security/SKILL.md

<interfaces>
<!-- Script Python de référence — SagePayrollImportService -->
<!-- Fourni directement par le client — implémenter l'équivalent TypeScript exact -->

```python
class SagePayrollImportService:
    def __init__(self):
        self.column_mapping = {
            'Jours de présence': 'days_worked',
            'Salaire de base': 'base_salary',
            'Sursalaire': 'bonus_salary',
            "Prime d'ancienneté": 'seniority_premium',
            'Indemnité de transport': 'transport_allowance',
            'Indemnité congés payés': 'vacation_allowance',
            'Heures supplémentaires': 'overtime_pay',
            '*** SALAIRE BRUT ***': 'gross_salary',
            '*** INDEMNITE EXONEREE ***': 'exempt_indemnity',
            '*** BRUT FISCAL ***': 'fiscal_gross',
            '*** BRUT SOCIAL ***': 'social_gross',
            'Impôts sur salaire (IS)': 'tax_is',
            'Contribution nationale (CN)': 'tax_cn',
            'Impôt général sur revenu (IGR)': 'tax_igr',
            'Retenue CNPS': 'withholding_cnps',
            '*** TOTAL DES COTISATIONS ***': 'total_contributions',
            '*** NET AVANT RETENUE ***': 'net_before_withholding',
            'Reprise arrondi de paie (M-1)': 'adjustment_m_minus_1',
            'Reprise paie négative (M-1)': 'negative_pay_adjustment',
            'Avance paie négative': 'negative_advance',
            'Arrondi de paie': 'rounding_adjustment',
            'NET A PAYER': 'net_to_pay'
        }

    def clean_currency(self, value):
        if pd.isna(value) or value == '': return 0.0
        val_str = str(value).replace(' ', '').replace('\xa0', '').replace(' ', '').replace(',', '.')
        try:
            return float(val_str)
        except ValueError:
            return 0.0

    def process_payroll_file(self, file_path):
        df = pd.read_excel(file_path, header=7)  # ← header=7 pour fichier Sage natif
        df['employee_id'] = df.iloc[:, 0].apply(lambda x: str(x).split(' ')[0])
        df['employee_name'] = df.iloc[:, 0].apply(lambda x: " ".join(str(x).split(' ')[1:]))
        for sage_col, sirh_col in self.column_mapping.items():
            if sage_col in df.columns:
                df[sirh_col] = df[sage_col].apply(self.clean_currency)
        return df[['employee_id', 'employee_name'] + list(self.column_mapping.values())]
```

<!-- Modèle parseSalary existant dans la codebase (app/api/import/employees/route.ts) -->
```typescript
function parseSalary(val: unknown): number {
  if (typeof val === "number") return val;
  if (!val) return 0;
  let s = String(val).toUpperCase().replace(/[\s﻿\xA0FCFA]/g, "");
  s = s.replace(",", ".");
  const parts = s.split(".");
  if (parts.length > 2 || (parts.length === 2 && parts[1].length === 3)) {
    s = parts.join("");
  }
  const parsed = parseFloat(s);
  return isNaN(parsed) || parsed < 0 ? 0 : parsed;
}
```

<!-- Pattern RLS migration existant -->
```sql
ALTER TABLE ma_table ENABLE ROW LEVEL SECURITY;
CREATE POLICY "isolation_company" ON ma_table
  USING (company_id = get_user_company_id());
CREATE INDEX idx_... ON ma_table(company_id, ...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL payroll_logs — 22 colonnes complètes + RLS</name>
  <read_first>
    - supabase/migrations/ (lister pour vérifier le timestamp le plus récent — éviter conflit de nom)
    - skills/database/SKILL.md (pattern RLS obligatoire)
  </read_first>
  <files>supabase/migrations/20260427000000_payroll_logs.sql</files>
  <action>
Créer le fichier de migration SQL suivant en copiant exactement ce DDL :

```sql
-- Migration : table payroll_logs pour l'import Sage
-- Phase 8 — Import Paie Sage
-- ISP-04 : traçabilité complète, aucune autre table modifiée
-- Colonnes calquées sur SagePayrollImportService.column_mapping (Python)

CREATE TABLE IF NOT EXISTS payroll_logs (
  -- Clé primaire et isolation multi-tenant
  id                      UUID          DEFAULT gen_random_uuid() PRIMARY KEY,
  company_id              UUID          NOT NULL REFERENCES companies(id),

  -- Identité employé (valeur brute Sage — TEXT, pas UUID FK)
  employee_id             TEXT          NOT NULL,  -- premier mot de "Matricule/Nom" (ex: "EMP001")
  employee_name           TEXT,                     -- reste de "Matricule/Nom" (ex: "KOFFI Jean")

  -- Champs salariaux (22 colonnes du mapping SagePayrollImportService)
  days_worked             NUMERIC(10, 2),           -- Jours de présence
  base_salary             NUMERIC(14, 2),           -- Salaire de base
  bonus_salary            NUMERIC(14, 2),           -- Sursalaire
  seniority_premium       NUMERIC(14, 2),           -- Prime d'ancienneté
  transport_allowance     NUMERIC(14, 2),           -- Indemnité de transport
  vacation_allowance      NUMERIC(14, 2),           -- Indemnité congés payés
  overtime_pay            NUMERIC(14, 2),           -- Heures supplémentaires
  gross_salary            NUMERIC(14, 2),           -- *** SALAIRE BRUT ***
  exempt_indemnity        NUMERIC(14, 2),           -- *** INDEMNITE EXONEREE ***
  fiscal_gross            NUMERIC(14, 2),           -- *** BRUT FISCAL ***
  social_gross            NUMERIC(14, 2),           -- *** BRUT SOCIAL ***
  tax_is                  NUMERIC(14, 2),           -- Impôts sur salaire (IS)
  tax_cn                  NUMERIC(14, 2),           -- Contribution nationale (CN)
  tax_igr                 NUMERIC(14, 2),           -- Impôt général sur revenu (IGR)
  withholding_cnps        NUMERIC(14, 2),           -- Retenue CNPS
  total_contributions     NUMERIC(14, 2),           -- *** TOTAL DES COTISATIONS ***
  net_before_withholding  NUMERIC(14, 2),           -- *** NET AVANT RETENUE ***
  adjustment_m_minus_1    NUMERIC(14, 2),           -- Reprise arrondi de paie (M-1)
  negative_pay_adjustment NUMERIC(14, 2),           -- Reprise paie négative (M-1)
  negative_advance        NUMERIC(14, 2),           -- Avance paie négative
  rounding_adjustment     NUMERIC(14, 2),           -- Arrondi de paie
  net_to_pay              NUMERIC(14, 2),           -- NET A PAYER

  -- Métadonnées d'import
  periode                 TEXT,                     -- Format YYYY-MM (mois importé)
  import_source           TEXT          NOT NULL DEFAULT 'sage',
  imported_at             TIMESTAMPTZ   NOT NULL DEFAULT now(),
  imported_by             UUID          REFERENCES auth.users(id)
);

ALTER TABLE payroll_logs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "isolation_company" ON payroll_logs
  USING (company_id = get_user_company_id());

CREATE INDEX IF NOT EXISTS idx_payroll_logs_company_period
  ON payroll_logs(company_id, periode);

CREATE INDEX IF NOT EXISTS idx_payroll_logs_company_employee
  ON payroll_logs(company_id, employee_id);
```

Notes :
- `employee_id` TEXT NOT NULL — pas de FK UUID (valeur brute Sage : premier mot de "Matricule/Nom")
- Toutes les colonnes numériques sont NULLABLE — un champ absent dans le fichier Sage reste NULL
- `IF NOT EXISTS` pour l'idempotence
- Pas de contrainte UNIQUE sur (company_id, employee_id, periode) — le double import affiche un avertissement côté API, il ne bloque pas
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE\|ENABLE ROW LEVEL\|isolation_company\|net_to_pay\|employee_name" "supabase/migrations/20260427000000_payroll_logs.sql"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260427000000_payroll_logs.sql existe
    - grep "CREATE TABLE IF NOT EXISTS payroll_logs" retourne 1 ligne
    - grep "ENABLE ROW LEVEL SECURITY" retourne 1 ligne
    - grep "net_to_pay" retourne 1 ligne (colonne finale du mapping)
    - grep "employee_name" retourne 1 ligne
    - grep "isolation_company" retourne 1 ligne (politique RLS)
    - Le fichier contient exactement 22 colonnes numériques (days_worked → net_to_pay)
  </acceptance_criteria>
  <done>Migration SQL créée avec les 22 colonnes, RLS et index</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/paie-sage-import.ts — équivalent TypeScript de SagePayrollImportService + tests TDD</name>
  <read_first>
    - app/api/import/employees/route.ts (fonction parseSalary ligne ~24-38 — modèle de base à adapter)
    - lib/ (lister — vérifier qu'il n'existe pas déjà un paie-sage-import.ts)
    - .planning/phases/08-import-sage-paie/08-CONTEXT.md (section "Script Python fourni")
  </read_first>
  <files>lib/paie-sage-import.ts, lib/__tests__/paie-sage-import.test.ts</files>
  <behavior>
    === cleanCurrency (équivalent de clean_currency Python) ===
    - cleanCurrency(350000) → 350000 (number natif passé tel quel)
    - cleanCurrency("350\xa0000") → 350000 (espace insécable \xa0)
    - cleanCurrency("350 000") → 350000 (espace fine insécable  )
    - cleanCurrency("1 234,56") → 1234.56 (espace + virgule décimale)
    - cleanCurrency("295,50") → 295.5 (virgule décimale simple)
    - cleanCurrency("") → 0 (chaîne vide → 0, comme le Python)
    - cleanCurrency(null) → 0 (null → 0)
    - cleanCurrency(undefined) → 0 (undefined → 0)
    - cleanCurrency("abc") → 0 (non numérique → 0)
    - cleanCurrency(0) → 0 (zéro est valide)
    - cleanCurrency(NaN) → 0 (NaN → 0)

    === extractEmployeeInfo (équivalent de la lambda Python) ===
    - extractEmployeeInfo("EMP001 KOFFI Jean") → { employee_id: "EMP001", employee_name: "KOFFI Jean" }
    - extractEmployeeInfo("EMP001 - KOFFI Jean") → { employee_id: "EMP001", employee_name: "- KOFFI Jean" }
    - extractEmployeeInfo("MAT042") → { employee_id: "MAT042", employee_name: "" }
    - extractEmployeeInfo("") → { employee_id: "", employee_name: "" }
    - extractEmployeeInfo(null) → { employee_id: "", employee_name: "" }

    === validateRequiredColumns ===
    - validateRequiredColumns(REQUIRED_COLUMNS, { "Jours de présence": "", "NET A PAYER": "", "Salaire de base": "" }) → null (colonnes requises présentes)
    - validateRequiredColumns(REQUIRED_COLUMNS, { "Jours de présence": "" }) → "Colonne 'NET A PAYER' manquante dans le fichier importé. Vérifiez que vous avez utilisé le template fourni." (première manquante)
  </behavior>
  <action>
RED : Créer lib/__tests__/paie-sage-import.test.ts avec tous les cas ci-dessus.
Lancer `npm test -- --reporter=verbose 2>&1 | head -40` — doit échouer (module introuvable).

GREEN : Créer lib/paie-sage-import.ts avec le contenu suivant (copier exactement) :

```typescript
// lib/paie-sage-import.ts
// Équivalent TypeScript de SagePayrollImportService (Python)
// Fourni par le client — ne pas modifier la logique métier

/**
 * Mapping complet des colonnes Sage vers les colonnes SIRH (payroll_logs).
 * Source : SagePayrollImportService.column_mapping
 */
export const COLUMN_MAPPING: Record<string, string> = {
  "Jours de présence": "days_worked",
  "Salaire de base": "base_salary",
  "Sursalaire": "bonus_salary",
  "Prime d'ancienneté": "seniority_premium",
  "Indemnité de transport": "transport_allowance",
  "Indemnité congés payés": "vacation_allowance",
  "Heures supplémentaires": "overtime_pay",
  "*** SALAIRE BRUT ***": "gross_salary",
  "*** INDEMNITE EXONEREE ***": "exempt_indemnity",
  "*** BRUT FISCAL ***": "fiscal_gross",
  "*** BRUT SOCIAL ***": "social_gross",
  "Impôts sur salaire (IS)": "tax_is",
  "Contribution nationale (CN)": "tax_cn",
  "Impôt général sur revenu (IGR)": "tax_igr",
  "Retenue CNPS": "withholding_cnps",
  "*** TOTAL DES COTISATIONS ***": "total_contributions",
  "*** NET AVANT RETENUE ***": "net_before_withholding",
  "Reprise arrondi de paie (M-1)": "adjustment_m_minus_1",
  "Reprise paie négative (M-1)": "negative_pay_adjustment",
  "Avance paie négative": "negative_advance",
  "Arrondi de paie": "rounding_adjustment",
  "NET A PAYER": "net_to_pay",
} as const;

/** Colonnes obligatoires pour valider le fichier importé. */
export const REQUIRED_COLUMNS = ["NET A PAYER", "Salaire de base"] as const;

/**
 * Nettoyage robuste des montants Sage (équivalent de clean_currency Python).
 * Gère : espaces insécables ( , \xa0), espaces normaux, virgules décimales.
 * Retourne 0 pour les valeurs invalides/vides (comportement identique au Python).
 */
export function cleanCurrency(value: unknown): number {
  if (value === null || value === undefined || value === "") return 0;
  if (typeof value === "number") return isNaN(value) ? 0 : value;
  const s = String(value)
    .replace(/ /g, "")   // espace fine insécable
    .replace(/\xa0/g, "")     // espace insécable
    .replace(/ /g, "")        // espace ordinaire
    .replace(",", ".");        // virgule décimale → point
  const n = parseFloat(s);
  return isNaN(n) ? 0 : n;
}

/**
 * Extrait l'identifiant et le nom depuis la valeur brute Sage de la colonne "Matricule/Nom".
 * Comportement Python : employee_id = premier mot, employee_name = reste.
 * Exemple : "EMP001 KOFFI Jean" → { employee_id: "EMP001", employee_name: "KOFFI Jean" }
 */
export function extractEmployeeInfo(rawValue: unknown): {
  employee_id: string;
  employee_name: string;
} {
  const raw = rawValue == null ? "" : String(rawValue).trim();
  if (!raw) return { employee_id: "", employee_name: "" };
  const parts = raw.split(" ");
  return {
    employee_id: parts[0] ?? "",
    employee_name: parts.slice(1).join(" "),
  };
}

/**
 * Vérifie que toutes les colonnes requises sont présentes dans la première ligne du fichier.
 * Retourne un message d'erreur pour la première colonne manquante, ou null si tout est présent.
 */
export function validateRequiredColumns(
  required: readonly string[],
  firstRow: Record<string, unknown>
): string | null {
  for (const col of required) {
    if (!(col in firstRow)) {
      return `Colonne '${col}' manquante dans le fichier importé. Vérifiez que vous avez utilisé le template fourni.`;
    }
  }
  return null;
}
```

Lancer `npm test -- --reporter=verbose 2>&1 | head -60` — doit passer à 100%.

REFACTOR : Vérifier qu'aucun `any` n'est utilisé.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | head -60</automated>
  </verify>
  <acceptance_criteria>
    - lib/paie-sage-import.ts existe et exporte cleanCurrency, extractEmployeeInfo, validateRequiredColumns, COLUMN_MAPPING, REQUIRED_COLUMNS
    - COLUMN_MAPPING contient exactement 22 entrées (grep "=>" lib/paie-sage-import.ts | wc -l retourne ≥ 22)
    - lib/__tests__/paie-sage-import.test.ts couvre ≥ 16 cas de test
    - npm test passe à 100% (aucun test rouge)
    - grep ": any" lib/paie-sage-import.ts retourne vide (aucun any)
  </acceptance_criteria>
  <done>
    - lib/paie-sage-import.ts créé avec les 5 exports
    - Tests TDD verts à 100%
    - COLUMN_MAPPING a 22 entrées correspondant exactement au Python
  </done>
</task>

<task type="auto">
  <name>Task 3: Mettre à jour types/supabase.ts avec le type PayrollLog complet</name>
  <read_first>
    - types/supabase.ts (lignes 1-50 pour voir le pattern Database{} → Tables → Row/Insert/Update)
    - supabase/migrations/20260427000000_payroll_logs.sql (colonnes exactes à typer)
  </read_first>
  <files>types/supabase.ts</files>
  <action>
Localiser dans types/supabase.ts la section `Tables:` à l'intérieur de `public: { Tables: {`. Ajouter l'entrée `payroll_logs` dans cette section, en ordre alphabétique, avec la structure suivante (copier exactement) :

```typescript
payroll_logs: {
  Row: {
    id: string;
    company_id: string;
    employee_id: string;
    employee_name: string | null;
    days_worked: number | null;
    base_salary: number | null;
    bonus_salary: number | null;
    seniority_premium: number | null;
    transport_allowance: number | null;
    vacation_allowance: number | null;
    overtime_pay: number | null;
    gross_salary: number | null;
    exempt_indemnity: number | null;
    fiscal_gross: number | null;
    social_gross: number | null;
    tax_is: number | null;
    tax_cn: number | null;
    tax_igr: number | null;
    withholding_cnps: number | null;
    total_contributions: number | null;
    net_before_withholding: number | null;
    adjustment_m_minus_1: number | null;
    negative_pay_adjustment: number | null;
    negative_advance: number | null;
    rounding_adjustment: number | null;
    net_to_pay: number | null;
    periode: string | null;
    import_source: string;
    imported_at: string;
    imported_by: string | null;
  };
  Insert: {
    id?: string;
    company_id: string;
    employee_id: string;
    employee_name?: string | null;
    days_worked?: number | null;
    base_salary?: number | null;
    bonus_salary?: number | null;
    seniority_premium?: number | null;
    transport_allowance?: number | null;
    vacation_allowance?: number | null;
    overtime_pay?: number | null;
    gross_salary?: number | null;
    exempt_indemnity?: number | null;
    fiscal_gross?: number | null;
    social_gross?: number | null;
    tax_is?: number | null;
    tax_cn?: number | null;
    tax_igr?: number | null;
    withholding_cnps?: number | null;
    total_contributions?: number | null;
    net_before_withholding?: number | null;
    adjustment_m_minus_1?: number | null;
    negative_pay_adjustment?: number | null;
    negative_advance?: number | null;
    rounding_adjustment?: number | null;
    net_to_pay?: number | null;
    periode?: string | null;
    import_source?: string;
    imported_at?: string;
    imported_by?: string | null;
  };
  Update: Partial<{
    id: string;
    company_id: string;
    employee_id: string;
    employee_name: string | null;
    days_worked: number | null;
    base_salary: number | null;
    bonus_salary: number | null;
    seniority_premium: number | null;
    transport_allowance: number | null;
    vacation_allowance: number | null;
    overtime_pay: number | null;
    gross_salary: number | null;
    exempt_indemnity: number | null;
    fiscal_gross: number | null;
    social_gross: number | null;
    tax_is: number | null;
    tax_cn: number | null;
    tax_igr: number | null;
    withholding_cnps: number | null;
    total_contributions: number | null;
    net_before_withholding: number | null;
    adjustment_m_minus_1: number | null;
    negative_pay_adjustment: number | null;
    negative_advance: number | null;
    rounding_adjustment: number | null;
    net_to_pay: number | null;
    periode: string | null;
    import_source: string;
    imported_at: string;
    imported_by: string | null;
  }>;
};
```

Ne modifier aucune autre entrée de types/supabase.ts.
  </action>
  <verify>
    <automated>grep -c "payroll_logs\|net_to_pay\|employee_name" "types/supabase.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep "payroll_logs" types/supabase.ts retourne ≥ 3 lignes (Row, Insert, Update)
    - grep "net_to_pay" types/supabase.ts retourne ≥ 3 lignes
    - grep "employee_name" types/supabase.ts retourne ≥ 3 lignes
    - npx tsc --noEmit 2>&1 | grep "payroll_logs" retourne vide (pas d'erreurs TS)
  </acceptance_criteria>
  <done>types/supabase.ts contient payroll_logs avec les 29 colonnes (Row/Insert/Update)</done>
</task>

</tasks>

<verification>
```bash
# 1. Migration SQL présente avec colonnes clés
grep -c "CREATE TABLE\|ENABLE ROW LEVEL\|isolation_company\|net_to_pay\|employee_name" \
  "supabase/migrations/20260427000000_payroll_logs.sql"
# Attendu : 5

# 2. COLUMN_MAPPING complet
grep -c "'days_worked'\|'net_to_pay'\|'gross_salary'\|'total_contributions'" \
  "lib/paie-sage-import.ts"
# Attendu : 4

# 3. Tests verts
npm test -- --reporter=verbose 2>&1 | tail -10
# Attendu : aucun test rouge, "passed" visible

# 4. Pas de 'any'
grep -n ": any\|as any" lib/paie-sage-import.ts
# Attendu : aucun résultat

# 5. Types mis à jour
grep -c "net_to_pay\|employee_name" types/supabase.ts
# Attendu : ≥ 6 (3 occurrences × Row/Insert/Update)
```
</verification>

<success_criteria>
- supabase/migrations/20260427000000_payroll_logs.sql : CREATE TABLE avec 22 colonnes numériques + employee_name + RLS
- lib/paie-sage-import.ts : COLUMN_MAPPING (22 entrées), cleanCurrency, extractEmployeeInfo, validateRequiredColumns, REQUIRED_COLUMNS — sans `any`
- lib/__tests__/paie-sage-import.test.ts : ≥ 16 cas couvrant cleanCurrency, extractEmployeeInfo, validateRequiredColumns
- types/supabase.ts : entrée payroll_logs complète avec Row/Insert/Update
- npm test passe à 100%
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-import-sage-paie/08-01-SUMMARY.md`
</output>
