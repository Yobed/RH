---
phase: 08-import-sage-paie
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/api/paie/import-sage/route.ts
  - app/api/paie/import-sage/template/route.ts
autonomous: true
requirements:
  - ISP-02
  - ISP-03
  - ISP-04
  - ISP-05

must_haves:
  truths:
    - "GET /api/paie/import-sage/template retourne un .xlsx avec les colonnes principales du COLUMN_MAPPING et une feuille Instructions"
    - "POST /api/paie/import-sage retourne 401 si l'utilisateur n'est pas authentifie"
    - "POST /api/paie/import-sage detecte automatiquement la ligne d'en-tete (header=7 pour Sage natif, header=0 pour template SIRH)"
    - "POST /api/paie/import-sage retourne 422 { success: false, errors } si colonne requise manquante — zero ecriture en base"
    - "POST /api/paie/import-sage mappe les 22 colonnes via COLUMN_MAPPING et appelle cleanCurrency sur chaque valeur"
    - "Chaque ligne inseree dans payroll_logs contient employee_id, employee_name, toutes les colonnes disponibles, company_id, imported_by"
  artifacts:
    - path: "app/api/paie/import-sage/template/route.ts"
      provides: "Route Handler GET — template Excel avec colonnes principales"
      exports: ["GET"]
    - path: "app/api/paie/import-sage/route.ts"
      provides: "Route Handler POST — detection header + mapping 22 colonnes + insert payroll_logs"
      exports: ["POST"]
  key_links:
    - from: "app/api/paie/import-sage/route.ts"
      to: "lib/paie-sage-import.ts"
      via: "import { cleanCurrency, extractEmployeeInfo, validateRequiredColumns, REQUIRED_COLUMNS, COLUMN_MAPPING }"
      pattern: "COLUMN_MAPPING"
    - from: "app/api/paie/import-sage/route.ts"
      to: "supabase payroll_logs"
      via: "supabase.from('payroll_logs').insert(toInsert)"
      pattern: "payroll_logs.*insert"
---

<objective>
Creer les deux Route Handlers : l'endpoint GET qui genere le template Excel, et l'endpoint POST qui recoit le fichier (Sage natif ou template SIRH), detecte automatiquement la ligne d'en-tete, mappe les 22 colonnes via COLUMN_MAPPING, nettoie les montants via cleanCurrency, et ecrit dans payroll_logs uniquement si zero erreur de validation.

Purpose: API complete de l'import — template telechargeable et traitement complet du fichier avec les 22 colonnes de SagePayrollImportService.
Output: app/api/paie/import-sage/template/route.ts (GET) et app/api/paie/import-sage/route.ts (POST).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-import-sage-paie/08-CONTEXT.md
@.planning/phases/08-import-sage-paie/08-RESEARCH.md
@.planning/phases/08-import-sage-paie/08-01-SUMMARY.md

<interfaces>
From app/api/import/employees/template/route.ts (modele GET a adapter) :
```typescript
import { NextResponse } from "next/server";
import * as XLSX from "xlsx";
export const dynamic = "force-dynamic";
// XLSX.utils.book_new() -> aoa_to_sheet([headers, example]) -> book_append_sheet
// -> XLSX.write(wb, {type:"buffer", bookType:"xlsx"})
// -> new NextResponse(buf, { headers: { "Content-Type": "...", "Content-Disposition": "..." }})
```

From app/api/import/employees/route.ts (modele POST a adapter) :
```typescript
// Auth :
const supabase = createServerClient();
const { data: { user } } = await supabase.auth.getUser();
if (!user) return NextResponse.json({ error: "Non authentifie" }, { status: 401 });
const { data: profile } = await supabase.from("profiles").select("company_id").eq("id", user.id).single();
if (!profile?.company_id) return NextResponse.json({ error: "Entreprise introuvable" }, { status: 400 });
// XLSX read :
const buf = await file.arrayBuffer();
const wb = XLSX.read(buf, { type: "array" });
const ws = wb.Sheets[wb.SheetNames[0]];
const rows = XLSX.utils.sheet_to_json(ws, { defval: "" });
```

Imports depuis lib/paie-sage-import.ts (Wave 1) :
```typescript
import {
  cleanCurrency,
  extractEmployeeInfo,
  validateRequiredColumns,
  REQUIRED_COLUMNS,
  COLUMN_MAPPING,
} from "@/lib/paie-sage-import";
```

Format de retour API :
```typescript
// Succes  : { success: true, imported: N, errors: [] }
// Erreur  : { success: false, errors: [{ row: number, message: string }] }
// Serveur : { error: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Handler GET — template Excel</name>
  <read_first>
    - app/api/import/employees/template/route.ts (modele exact a copier/adapter)
    - lib/paie-sage-import.ts (COLUMN_MAPPING — colonnes a inclure dans le template)
  </read_first>
  <files>app/api/paie/import-sage/template/route.ts</files>
  <action>
Creer app/api/paie/import-sage/template/ et route.ts en adaptant exactement app/api/import/employees/template/route.ts.

Structure du template (feuille "Paie Sage") :
- Ligne 1 (headers) : ["Matricule/Nom", "Jours de presence", "Salaire de base", "Sursalaire", "Prime d'anciennete", "Indemnite de transport", "Heures supplementaires", "*** SALAIRE BRUT ***", "*** BRUT SOCIAL ***", "NET A PAYER"]
- Ligne 2 (exemple)  : ["EMP001 KOFFI Jean", "26", "350000", "0", "15000", "20000", "0", "385000", "385000", "295000"]

Feuille 2 nommee "Instructions" :
- ["Instructions pour l'import Sage Paie", ""]
- ["1.", "Exportez votre livre de paie depuis Sage Paie"]
- ["2.", "Copiez-collez vos donnees dans les colonnes de la feuille 'Paie Sage'"]
- ["3.", "Sauvegardez et importez ce fichier via la page Import Paie Sage"]
- ["Colonnes obligatoires:", "Salaire de base, NET A PAYER"]
- ["Colonnes optionnelles:", "Toutes les autres colonnes du template"]
- ["Note header:", "Si vous importez un fichier Sage natif, le systeme detecte automatiquement la ligne d'en-tete"]

Largeur colonnes feuille "Paie Sage" : wch: 30 pour chaque colonne.
Largeur colonnes feuille "Instructions" : [{wch: 25}, {wch: 65}].

Content-Disposition : attachment; filename="template-import-sage.xlsx"
Content-Type : application/vnd.openxmlformats-officedocument.spreadsheetml.sheet

export const dynamic = "force-dynamic" en haut du fichier.
NE PAS ajouter "use client".
  </action>
  <verify>
    <automated>grep -n "Content-Disposition\|Paie Sage\|Matricule\|force-dynamic\|NET A PAYER" "app/api/paie/import-sage/template/route.ts"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/paie/import-sage/template/route.ts existe
    - grep "force-dynamic" retourne 1 ligne
    - grep "Content-Disposition" retourne 1 ligne contenant "template-import-sage.xlsx"
    - grep "Matricule" retourne au moins 1 ligne
    - grep "NET A PAYER" retourne au moins 1 ligne
    - grep "Instructions" retourne au moins 1 ligne (deuxieme feuille)
  </acceptance_criteria>
  <done>Route GET creee — template Excel avec feuilles Paie Sage et Instructions</done>
</task>

<task type="auto">
  <name>Task 2: Route Handler POST — detection header + mapping 22 colonnes + insert payroll_logs</name>
  <read_first>
    - app/api/import/employees/route.ts (pattern auth + XLSX read + boucle validation — modele direct)
    - lib/paie-sage-import.ts (tous les exports : cleanCurrency, extractEmployeeInfo, validateRequiredColumns, REQUIRED_COLUMNS, COLUMN_MAPPING)
    - skills/security/SKILL.md (auth en premier)
  </read_first>
  <files>app/api/paie/import-sage/route.ts</files>
  <action>
Creer app/api/paie/import-sage/route.ts. Implementer la logique suivante dans l'ordre strict :

1. AUTH
```typescript
export const dynamic = "force-dynamic";
const supabase = createServerClient();
const { data: { user } } = await supabase.auth.getUser();
if (!user) return NextResponse.json({ error: "Non authentifie" }, { status: 401 });
const { data: profile } = await supabase.from("profiles").select("company_id").eq("id", user.id).single();
if (!profile?.company_id) return NextResponse.json({ error: "Entreprise introuvable" }, { status: 400 });
```

2. PARSE FORM
```typescript
const formData = await request.formData();
const file = formData.get("file") as File | null;
if (!file) return NextResponse.json({ error: "Fichier manquant" }, { status: 400 });
const periode = formData.get("periode") as string | null;  // optionnel, format YYYY-MM
```

3. READ XLSX avec detection automatique du header
```typescript
const buf = await file.arrayBuffer();
const wb = XLSX.read(buf, { type: "array" });
const ws = wb.Sheets[wb.SheetNames[0]];

// Detection automatique : chercher la ligne contenant "Jours de presence"
// pour les fichiers Sage natifs (header=7), sinon header=0 (template SIRH)
const rawData = XLSX.utils.sheet_to_json<string[]>(ws, { header: 1, defval: "" });
const SAGE_MARKER = "Jours de présence";
let headerRowIndex = 0;
for (let i = 0; i < Math.min(rawData.length, 15); i++) {
  const row = rawData[i] as unknown[];
  if (row.some((cell) => String(cell).trim() === SAGE_MARKER)) {
    headerRowIndex = i;
    break;
  }
}
const rows = XLSX.utils.sheet_to_json<Record<string, unknown>>(ws, {
  range: headerRowIndex,
  defval: "",
});
```

4. CHECKS BASIQUES
```typescript
if (rows.length === 0) return NextResponse.json({ success: false, errors: [{ row: 0, message: "Le fichier est vide ou ne contient pas de donnees." }] }, { status: 400 });
if (rows.length > 1000) return NextResponse.json({ success: false, errors: [{ row: 0, message: "Le fichier contient plus de 1000 lignes. Importez par tranches de 1000 lignes maximum." }] }, { status: 422 });
```

5. VALIDATION COLONNES
```typescript
const colError = validateRequiredColumns(REQUIRED_COLUMNS, rows[0]);
if (colError) return NextResponse.json({ success: false, errors: [{ row: 0, message: colError }] }, { status: 422 });
```

6. VALIDATION LIGNE PAR LIGNE + MAPPING 22 COLONNES
```typescript
const errors: { row: number; message: string }[] = [];
const toInsert: Record<string, unknown>[] = [];

for (let i = 0; i < rows.length; i++) {
  const row = rows[i];
  const rowNum = i + headerRowIndex + 2;
  const { employee_id, employee_name } = extractEmployeeInfo(row["Matricule/Nom"]);

  if (!employee_id) {
    errors.push({ row: rowNum, message: `Ligne ${rowNum}: Matricule/Nom vide ou invalide.` });
    continue;
  }

  // Mapper toutes les colonnes disponibles via COLUMN_MAPPING
  const mappedFields: Record<string, number> = {};
  for (const [sageCol, sirhCol] of Object.entries(COLUMN_MAPPING)) {
    if (sageCol in row) {
      mappedFields[sirhCol] = cleanCurrency(row[sageCol]);
    }
  }

  // Valider les colonnes obligatoires
  if (!("base_salary" in mappedFields)) {
    errors.push({ row: rowNum, message: `Ligne ${rowNum}: 'Salaire de base' non numerique ou absent.` });
    continue;
  }
  if (!("net_to_pay" in mappedFields)) {
    errors.push({ row: rowNum, message: `Ligne ${rowNum}: 'NET A PAYER' non numerique ou absent.` });
    continue;
  }

  toInsert.push({
    company_id: profile.company_id,
    employee_id,
    employee_name: employee_name || null,
    ...mappedFields,
    periode: periode ?? null,
    import_source: "sage",
    imported_at: new Date().toISOString(),
    imported_by: user.id,
  });
}
```

7. SI ERREURS — retour immediat sans insert
```typescript
if (errors.length > 0) {
  return NextResponse.json({ success: false, errors }, { status: 422 });
}
```

8. INSERT
```typescript
const { error: insertError } = await supabase.from("payroll_logs").insert(toInsert);
if (insertError) return NextResponse.json({ error: insertError.message }, { status: 500 });
```

9. SUCCES
```typescript
return NextResponse.json({ success: true, imported: toInsert.length, errors: [] });
```

Anti-patterns a eviter :
- NE PAS utiliser header: 7 en dur — utiliser la detection automatique ci-dessus
- NE PAS utiliser SUPABASE_SERVICE_ROLE_KEY
- NE PAS utiliser "any" TypeScript (Record<string, unknown> a la place)
- NE PAS ecrire partiellement (si errors.length > 0, retour immediat)
  </action>
  <verify>
    <automated>grep -n "payroll_logs\|COLUMN_MAPPING\|cleanCurrency\|extractEmployeeInfo\|createServerClient\|force-dynamic\|headerRowIndex" "app/api/paie/import-sage/route.ts"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/paie/import-sage/route.ts existe
    - grep "COLUMN_MAPPING" retourne au moins 2 lignes (import + usage dans Object.entries)
    - grep "cleanCurrency" retourne au moins 2 lignes (import + appel)
    - grep "extractEmployeeInfo" retourne au moins 2 lignes (import + appel)
    - grep "payroll_logs" retourne au moins 1 ligne (insert)
    - grep "headerRowIndex" retourne au moins 3 lignes (detection automatique du header)
    - grep "errors.length > 0" retourne 1 ligne (transaction applicative — retour sans insert)
    - grep ": any\|as any" app/api/paie/import-sage/route.ts retourne vide
    - npx tsc --noEmit 2>&1 | grep "import-sage/route" retourne vide (pas d'erreurs TS)
  </acceptance_criteria>
  <done>
    - Route POST creee avec detection automatique du header (Sage natif ou template SIRH)
    - Mapping des 22 colonnes via COLUMN_MAPPING
    - Transaction applicative : zero ecriture si errors.length > 0
    - Aucun "any" TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Template route
grep -c "Content-Disposition\|Paie Sage\|NET A PAYER\|force-dynamic\|Instructions" \
  "app/api/paie/import-sage/template/route.ts"
# Attendu : 5

# 2. Import route — imports Wave 1
grep "COLUMN_MAPPING\|cleanCurrency\|extractEmployeeInfo" "app/api/paie/import-sage/route.ts"
# Attendu : 3 lignes d'import + usages

# 3. Import route — securite et integrite
grep -n "payroll_logs\|getUser\|401\|errors.length\|headerRowIndex" "app/api/paie/import-sage/route.ts"
# Attendu : >= 5 lignes

# 4. Pas de "any" TypeScript
grep -n ": any\|as any" "app/api/paie/import-sage/route.ts"
# Attendu : aucun resultat

# 5. TypeScript compile
npx tsc --noEmit 2>&1 | head -20
# Attendu : aucune erreur sur les nouveaux fichiers

# 6. Tests toujours verts
npm test 2>&1 | tail -5
# Attendu : passed
```
</verification>

<success_criteria>
- app/api/paie/import-sage/template/route.ts : GET retourne .xlsx avec colonnes principales + feuille Instructions
- app/api/paie/import-sage/route.ts : detection automatique du header (Sage natif header=7, template SIRH header=0), mapping 22 colonnes via COLUMN_MAPPING, cleanCurrency sur chaque valeur, insert payroll_logs uniquement si zero erreur
- Aucun "any" TypeScript dans les deux fichiers
- npm test passe (aucune regression)
- npx tsc --noEmit sans erreur sur les nouveaux fichiers
</success_criteria>

<output>
Apres completion, creer `.planning/phases/08-import-sage-paie/08-02-SUMMARY.md`
</output>
