---
phase: 03-conges-absences
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260401000000_leave_balances.sql
  - lib/conges-ci.ts
  - app/api/conges/balance/route.ts
  - app/(dashboard)/employes/[id]/page.tsx
  - types/supabase.ts
autonomous: true
requirements:
  - CON-01

must_haves:
  truths:
    - "Un employé embauché il y a 6 mois a exactement 15 jours de solde acquis (6 × 2,5 j)"
    - "Le solde restant tient compte des congés annuels déjà pris sur l'année en cours"
    - "Le solde est visible sur la fiche employé"
    - "La correction légale 2,5 j/mois remplace l'ancienne valeur erronée (2,2 j)"
  artifacts:
    - path: "supabase/migrations/20260401000000_leave_balances.sql"
      provides: "Table leave_balances avec RLS"
    - path: "lib/conges-ci.ts"
      provides: "calculerSoldeConges() — logique légale CI"
    - path: "app/api/conges/balance/route.ts"
      provides: "GET /api/conges/balance?employee_id=xxx"
  key_links:
    - from: "app/(dashboard)/employes/[id]/page.tsx"
      to: "/api/conges/balance"
      via: "fetch server-side dans le Server Component"
    - from: "lib/conges-ci.ts"
      to: "leave_balances"
      via: "upsert dans l'API POST /api/conges/balance/recalcul"
---

<objective>
Poser la fondation de données du module congés : table `leave_balances`, logique de calcul du solde légal ivoirien (2,5 j/mois travaillé, Art. 25 CT-CI), et affichage du solde sur la fiche employé.

Purpose: Sans solde fiable, aucun autre plan de la phase ne peut fonctionner correctement. Le calcul du solde est la source de vérité pour le workflow de validation, le calendrier, et le bulletin.
Output: Migration SQL versionnée, module `lib/conges-ci.ts`, API GET balance, widget solde sur fiche employé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conges-absences/03-CONTEXT.md

<interfaces>
<!-- Table conges existante (types/supabase.ts) -->
```typescript
// conges.Row
{
  id: string
  company_id: string
  employee_id: string
  type: string         // "annuel" | "maladie" | "maternite" | "paternite" | "sans_solde" | "exceptionnel"
  date_debut: string
  date_fin: string
  nb_jours: number
  statut: string | null  // "demande" | "approuve" | "refuse"
  commentaire: string | null
  created_at: string | null
}

// employees.Row (champs pertinents)
{
  id: string
  company_id: string
  full_name: string
  matricule: string
  date_embauche: string  // ISO date
  departement: string | null
  statut: string | null
}

```

<!-- lib/paie-ci.ts — exports existants (à ne pas toucher) -->
```typescript
export function calculerSoldeDeCompte(params: ParametresSoldeDeCompte): ResultatSoldeDeCompte
// jours_conges_restants est un paramètre — ce plan doit le calculer
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Migration SQL leave_balances + module lib/conges-ci.ts</name>
  <files>supabase/migrations/20260401000000_leave_balances.sql, lib/conges-ci.ts</files>
  <behavior>
    - calculerJoursAcquis(dateEmbauche: string, annee: number): number
      → Employé embauché 01/07/2025, annee=2025 → 6 mois × 2,5 = 15.0 j
      → Employé embauché 01/01/2025, annee=2025 → 12 mois × 2,5 = 30.0 j
      → Employé embauché 15/12/2025, annee=2025 → 0 mois complets = 0 j (embauche fin décembre)
      → Plafonner à 30 j/an (base légale, sans bonus ancienneté dans v1)
    - calculerSoldeConges(jours_acquis: number, jours_pris: number): number
      → 15 acquis, 5 pris → 10 restants
      → jamais négatif (Math.max(0, ...))
  </behavior>
  <action>
    1. Créer `supabase/migrations/20260401000000_leave_balances.sql` :
       ```sql
       CREATE TABLE IF NOT EXISTS leave_balances (
         id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
         employee_id UUID NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
         annee INTEGER NOT NULL,
         jours_acquis NUMERIC(5,1) NOT NULL DEFAULT 0,
         jours_pris NUMERIC(5,1) NOT NULL DEFAULT 0,
         solde NUMERIC(5,1) GENERATED ALWAYS AS (GREATEST(0, jours_acquis - jours_pris)) STORED,
         updated_at TIMESTAMPTZ DEFAULT now(),
         CONSTRAINT leave_balances_unique UNIQUE (company_id, employee_id, annee)
       );
       ALTER TABLE leave_balances ENABLE ROW LEVEL SECURITY;
       CREATE POLICY "Isolation entreprise leave_balances"
         ON leave_balances FOR ALL
         USING (company_id = get_user_company_id())
         WITH CHECK (company_id = get_user_company_id());
       CREATE INDEX idx_leave_balances_employee ON leave_balances(employee_id, annee);
       ```
    2. Créer `lib/conges-ci.ts` avec :
       - `calculerJoursAcquis(dateEmbauche: string, annee: number): number`
         Logique : compter les mois complets travaillés dans `annee`. Un mois est complet si l'employé était présent le 1er du mois. Multiplier par 2,5, plafonner à 30.
         Utiliser `new Date()` avec timezone Africa/Abidjan (Intl).
       - `calculerSoldeConges(joursAcquis: number, jours_pris: number): number`
       - Type `SoldeConges { jours_acquis: number; jours_pris: number; solde: number; annee: number }`
    3. Exporter les deux fonctions depuis `lib/conges-ci.ts`. Aucun `any`.
    4. Corriger la page `/conges/page.tsx` : remplacer "2,2 jours/mois" par "2,5 jours/mois (Art. 25 CT-CI)"
  </action>
  <verify>
    <automated>npx vitest run lib/conges-ci.test.ts 2>/dev/null || echo "TEST FILE MISSING — créer lib/conges-ci.test.ts avant l'implémentation"</automated>
  </verify>
  <done>
    Tests passent : 3 cas calculerJoursAcquis + 2 cas calculerSoldeConges. Migration SQL valide syntaxiquement (pas d'erreur psql --dry-run).
  </done>
</task>

<task type="auto">
  <name>Task 2 : API GET/POST balance + affichage fiche employé</name>
  <files>app/api/conges/balance/route.ts, app/(dashboard)/employes/[id]/page.tsx, types/supabase.ts</files>
  <action>
    1. Créer `app/api/conges/balance/route.ts` :
       - `GET ?employee_id=UUID&annee=YYYY` : retourne le leave_balance de l'employé pour l'année. Si aucune ligne n'existe, calculer avec `calculerJoursAcquis()` + total des `conges` approuvés de l'année et insérer (upsert). Filtrer par `company_id` via RLS. Retourner `SoldeConges | null`.
       - `POST` (body `{ employee_id: string, annee: number }`) : force le recalcul (upsert). Utile pour la cron mensuelle future.
       - Auth obligatoire (401 si non connecté). Pas de `any`.

    2. Mettre à jour `app/(dashboard)/employes/[id]/page.tsx` (Server Component) :
       - Ajouter une requête parallèle à `leave_balances` via le client Supabase server (pas via l'API route — accès direct en server component).
       - Si aucune ligne, calculer avec `calculerJoursAcquis()` côté serveur et afficher le résultat calculé (pas d'upsert au rendu — écriture uniquement via l'API).
       - Afficher un widget "Solde congés [annee]" avec : Acquis : X j / Pris : Y j / Restant : Z j (badge coloré : vert si solde > 5, orange si 1-5, rouge si 0).
       - Les jours pris = somme des `conges` de type `annuel` avec `statut = 'approuve'` pour l'année en cours.

    3. Mettre à jour `types/supabase.ts` pour inclure la table `leave_balances` :
       ```typescript
       leave_balances: {
         Row: {
           id: string
           company_id: string
           employee_id: string
           annee: number
           jours_acquis: number
           jours_pris: number
           solde: number
           updated_at: string | null
         }
         Insert: { company_id: string; employee_id: string; annee: number; jours_acquis: number; jours_pris: number; id?: string; updated_at?: string | null }
         Update: { jours_acquis?: number; jours_pris?: number; updated_at?: string | null }
         Relationships: [...]
       }
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    `npx tsc --noEmit` passe sans erreur. Sur la fiche d'un employé embauché il y a 6 mois, le widget affiche "Acquis : 15,0 j / Pris : X j / Restant : Y j".
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zéro erreur TypeScript
2. `npx vitest run lib/conges-ci.test.ts` — tous les tests passent
3. La page `/employes/[id]` affiche le widget solde congés sans erreur de rendu
4. La page `/conges` affiche "2,5 j/mois" (plus "2,2 j")
</verification>

<success_criteria>

- Table `leave_balances` créée avec RLS, contrainte UNIQUE (company_id, employee_id, annee)
- `calculerJoursAcquis(dateEmbauche, annee)` retourne exactement 2,5 × mois complets (plafond 30)
- Widget solde visible sur la fiche employé avec couleur selon solde restant
- `GET /api/conges/balance?employee_id=X` retourne le solde calculé ou persisté
- TypeScript strict — zéro `any`
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-conges-absences/03-01-SUMMARY.md` avec :
- Fichiers créés/modifiés
- Décisions prises (ex: calcul mois complets, choix colonne GENERATED)
- Interfaces exportées depuis `lib/conges-ci.ts`
</output>
