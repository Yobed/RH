---
phase: 03-conges-absences
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - lib/paie-ci.ts
  - app/(dashboard)/paie/page.tsx
  - components/rh/PaieDialog.tsx
autonomous: true
requirements:
  - CON-04

must_haves:
  truths:
    - "calculerRetenuAbsence(3, 300000) retourne 34615 FCFA (300000 / 26 × 3)"
    - "Un bulletin pour un employé ayant 3 jours d'absence non justifiée affiche une ligne 'Retenue absence' avec le montant correct"
    - "Un employé sans absence n'a pas de ligne retenue absence sur son bulletin"
    - "La retenue est incluse dans le total des retenues du bulletin"
  artifacts:
    - path: "lib/paie-ci.ts"
      provides: "calculerRetenuAbsence(nbJours, salaireBrut): number"
    - path: "components/rh/PaieDialog.tsx"
      provides: "Champ nb_jours_absence + affichage ligne retenue dans la preview"
  key_links:
    - from: "components/rh/PaieDialog.tsx"
      to: "lib/paie-ci.ts calculerBulletinComplet"
      via: "nb_jours_absence passé dans LignesBulletin → total_retenues"
---

<objective>
Ajouter la fonction `calculerRetenuAbsence` dans `lib/paie-ci.ts` et l'intégrer dans `PaieDialog` pour que les absences non justifiées génèrent automatiquement une retenue proportionnelle dans le bulletin de paie.

Purpose: Un bulletin sans retenue d'absence pour un employé absent est un bulletin non conforme. La retenue doit être calculée automatiquement dès que le RH saisit des jours d'absence.
Output: Fonction pure testée, champ UI dans PaieDialog, ligne de retenue dans la preview du bulletin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-conges-absences/03-CONTEXT.md
@.planning/phases/03-conges-absences/03-01-SUMMARY.md
@lib/paie-ci.ts

<interfaces>
<!-- Interfaces existantes dans lib/paie-ci.ts à étendre -->
```typescript
// LignesBulletin (existant) — ajouter le champ optionnel :
export interface LignesBulletin {
  salaire_brut: number
  sursalaire?: number
  prime_transport?: number
  prime_anciennete?: number
  prime_fonction?: number
  prime_salissure?: number
  prime_depassement?: number
  prime_exceptionnelle?: number
  heures_sup?: { h15: number; h50: number; h75: number }
  autres_retenues?: number
  avances?: number
  nb_jours_absence?: number   // NOUVEAU — jours absence non justifiée
}

// ResultatPaieComplet (existant) — ajouter le champ :
export interface ResultatPaieComplet {
  total_brut: number
  total_imposable: number
  cnps_salarie: number
  cmu: number
  its: number
  total_retenues: number
  salaire_net: number
  heures_sup_montant: number
  retenu_absence: number       // NOUVEAU
}

```

<!-- Formule légale CI -->
// Salaire journalier = salaire_brut / 26 jours ouvrables (standard cabinet comptable CI)
// Retenue = Math.round(salaire_brut / 26 * nb_jours_absence)
// Plancher 0 (nb_jours_absence négatif ou nul → 0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : calculerRetenuAbsence dans lib/paie-ci.ts</name>
  <files>lib/paie-ci.ts</files>
  <behavior>
    - calculerRetenuAbsence(nbJoursAbsence: number, salaireBrut: number): number
      → (3, 300000) → Math.round(300000 / 26 * 3) = 34615
      → (0, 300000) → 0
      → (-1, 300000) → 0  (cas défensif)
      → (26, 100000) → 100000 (mois entier)
      → (1, 75000) → Math.round(75000 / 26) = 2885 (SMIG CI)
    - calculerBulletinComplet avec nb_jours_absence=3, salaire_brut=300000 :
      → retenu_absence = 34615
      → total_retenues inclut les 34615 en plus des autres retenues
      → salaire_net diminué d'autant
  </behavior>
  <action>
    1. Ajouter dans `lib/paie-ci.ts` après les exports existants :
       ```typescript
       /**
        * Retenue pour absence non justifiée — droit CI
        * Base : salaire journalier = salaire_brut / 26 jours ouvrables
        */
       export function calculerRetenuAbsence(nbJoursAbsence: number, salaireBrut: number): number {
         if (nbJoursAbsence <= 0 || salaireBrut <= 0) return 0
         return Math.round((salaireBrut / 26) * nbJoursAbsence)
       }
       ```
    2. Étendre `LignesBulletin` : ajouter `nb_jours_absence?: number`.
    3. Étendre `ResultatPaieComplet` : ajouter `retenu_absence: number`.
    4. Dans `calculerBulletinComplet` :
       - Calculer `const retenu_absence = calculerRetenuAbsence(lignes.nb_jours_absence ?? 0, lignes.salaire_brut)`
       - Ajouter `retenu_absence` dans `total_retenues` (après `autres_retenues`)
       - Retourner `retenu_absence` dans le résultat
  </action>
  <verify>
    <automated>npx vitest run lib/paie-ci.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Tous les tests paie-ci passent (incluant les nouveaux cas calculerRetenuAbsence). `npx tsc --noEmit` sans erreur.
  </done>
</task>

<task type="auto">
  <name>Task 2 : Champ absence dans PaieDialog + ligne retenue dans preview</name>
  <files>components/rh/PaieDialog.tsx, app/(dashboard)/paie/page.tsx</files>
  <action>
    1. Dans `components/rh/PaieDialog.tsx` :
       - Ajouter dans le schéma Zod : `nb_jours_absence: z.number().min(0).max(31).optional()` avec `defaultValues: { nb_jours_absence: 0 }`.
       - Ajouter un champ numérique "Jours d'absence non justifiée" dans la section "Retenues" du formulaire (après "Autres retenues"), avec label et placeholder "0".
       - Dans la preview du bulletin : si `nb_jours_absence > 0`, afficher une ligne "Retenue absence (Xj)" avec le montant calculé par `calculerRetenuAbsence()` en rouge dans le tableau des retenues.
       - Passer `nb_jours_absence` dans le body du POST/PATCH (stocker dans `details` JSONB si la colonne `bulletins.details` est disponible, sinon ajouter une colonne `nb_jours_absence INTEGER DEFAULT 0` via une mini-migration `20260401020000_bulletin_absence.sql`).

    2. Vérifier que le champ `nb_jours_absence` est bien transmis dans `calculerBulletinComplet()` lors du calcul de la preview (même pattern que `heures_sup`).

    3. Aucun `any`. Garder le formatage FCFA existant pour la ligne de retenue.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    Dans PaieDialog, saisir 3 jours d'absence pour un employé à 300 000 FCFA brut affiche "Retenue absence (3j) : -34 615 FCFA" dans la preview. Le salaire net diminue en conséquence. Un employé sans absence n'affiche pas cette ligne.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run lib/paie-ci.test.ts` — tous les tests passent (incluant calculerRetenuAbsence)
2. `npx tsc --noEmit` — zéro erreur
3. PaieDialog : champ "Jours d'absence" présent dans la section Retenues
4. Preview bulletin : ligne retenue apparaît si > 0 jours, absente si 0 jours
5. Calcul : 3 jours × (300 000 / 26) = 34 615 FCFA affiché correctement
</verification>

<success_criteria>
- `calculerRetenuAbsence(3, 300000)` retourne exactement 34615
- `calculerBulletinComplet` intègre la retenue dans `total_retenues` et `salaire_net`
- Preview PaieDialog affiche la ligne retenue conditionnellement
- Tests unitaires couvrent 5 cas (incluant 0 et SMIG)
- Zéro `any` TypeScript
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-conges-absences/03-04-SUMMARY.md` avec :
- Signature finale de calculerRetenuAbsence
- Extensions apportées à LignesBulletin et ResultatPaieComplet
- Migration SQL créée si nécessaire pour nb_jours_absence
</output>
