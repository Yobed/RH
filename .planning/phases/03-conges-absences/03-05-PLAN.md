---
phase: 03-conges-absences
plan: "05"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-04"
files_modified:
  - supabase/migrations/20260401030000_arret_maladie.sql
  - components/rh/ArretMaladieDialog.tsx
  - app/api/conges/arret/route.ts
  - app/(dashboard)/conges/page.tsx
  - types/supabase.ts
autonomous: true
requirements:
  - CON-05
  - CON-06

must_haves:
  truths:
    - "Un RH peut déclarer un arrêt maladie avec durée, justificatif PDF/image et type (maladie ordinaire / AT)"
    - "Un arrêt maladie avec justificatif validé n'entraîne pas de retenue salariale dans le bulletin"
    - "Un arrêt maladie sans justificatif (non justifié) génère une retenue via calculerRetenuAbsence"
    - "Le statut 'arret_maladie' est distinct dans la liste des congés et dans le workflow"
    - "Le justificatif est stocké dans Supabase Storage bucket 'documents'"
  artifacts:
    - path: "supabase/migrations/20260401030000_arret_maladie.sql"
      provides: "Colonnes justificatif_url + est_justifie + est_at sur table conges"
    - path: "components/rh/ArretMaladieDialog.tsx"
      provides: "Formulaire déclaration arrêt maladie avec upload justificatif"
    - path: "app/api/conges/arret/route.ts"
      provides: "POST /api/conges/arret — création arrêt avec upload Supabase Storage"
  key_links:
    - from: "ArretMaladieDialog.tsx"
      to: "POST /api/conges/arret"
      via: "FormData (multipart) — fichier + métadonnées"
    - from: "app/(dashboard)/conges/page.tsx"
      to: "est_justifie + est_at"
      via: "affichage badge 'Justifié' / 'AT' / 'Non justifié' dans la liste"
    - from: "components/rh/PaieDialog.tsx"
      to: "conges.est_justifie"
      via: "Note: le RH doit renseigner manuellement nb_jours_absence pour les non-justifiés (v1)"
---

<objective>
Ajouter la gestion des arrêts maladie et accidents de travail en étendant la table `conges` avec les champs nécessaires (justificatif, type AT) et en créant un formulaire de déclaration dédié avec upload de pièce justificative.

Purpose: Un arrêt maladie avec certificat médical ne doit pas générer de retenue — c'est une obligation légale CI. Le module doit permettre de distinguer absence injustifiée, arrêt maladie justifié, et accident de travail (qui déclenche la procédure CNPS).
Output: Migration SQL, formulaire ArretMaladieDialog, API arret, affichage enrichi dans la liste congés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-conges-absences/03-CONTEXT.md
@.planning/phases/03-conges-absences/03-02-SUMMARY.md
@.planning/phases/03-conges-absences/03-04-SUMMARY.md

<interfaces>
<!-- Nouvelles colonnes sur table conges -->
```sql
justificatif_url TEXT           -- URL Supabase Storage du document
est_justifie BOOLEAN DEFAULT false
est_at BOOLEAN DEFAULT false    -- Accident de travail reconnu CNPS
```

<!-- Path Storage -->
// documents/{company_id}/{employee_id}/Médical/{timestamp}_{filename}
// Bucket: "documents" (existant)

<!-- Type étendu conges -->
// Ajouter "arret_maladie" dans les types acceptés
// (les types existants : annuel, maladie, maternite, paternite, sans_solde, exceptionnel)
// Distinguer "maladie" (congé maladie sans certificat) et "arret_maladie" (avec certificat médical)

<!-- Règle bulletin v1 -->
// Le RH saisit nb_jours_absence dans PaieDialog = jours d'absence NON justifiés uniquement
// Les arrêts maladie avec est_justifie=true ne doivent PAS être comptés dans nb_jours_absence
// Note informative dans l'UI pour guider le RH

<!-- FormData body pour POST /api/conges/arret -->
// employee_id: UUID
// date_debut: string
// date_fin: string
// nb_jours: number
// est_at: boolean
// commentaire?: string
// justificatif?: File (optionnel en POST — peut être ajouté après)

```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Migration SQL + types + API POST /api/conges/arret</name>
  <files>supabase/migrations/20260401030000_arret_maladie.sql, app/api/conges/arret/route.ts, types/supabase.ts</files>
  <action>
    1. Créer `supabase/migrations/20260401030000_arret_maladie.sql` :
       ```sql
       ALTER TABLE conges
         ADD COLUMN IF NOT EXISTS justificatif_url TEXT,
         ADD COLUMN IF NOT EXISTS est_justifie BOOLEAN NOT NULL DEFAULT false,
         ADD COLUMN IF NOT EXISTS est_at BOOLEAN NOT NULL DEFAULT false;

       -- Mettre à jour la contrainte CHECK statut pour inclure arret_maladie dans les types
       -- (type est une colonne TEXT, pas ENUM — pas de contrainte à modifier)
       -- Ajouter un commentaire de documentation
       COMMENT ON COLUMN conges.est_at IS 'Accident de travail reconnu CNPS — pas de retenue salariale';
       COMMENT ON COLUMN conges.est_justifie IS 'Justificatif médical validé — pas de retenue salariale';
       ```

    2. Mettre à jour `types/supabase.ts` : ajouter `justificatif_url: string | null`, `est_justifie: boolean`, `est_at: boolean` dans `conges.Row`, `Insert`, `Update`.

    3. Créer `app/api/conges/arret/route.ts` (POST uniquement) :
       - Accepter `FormData` (multipart) pour gérer l'upload de fichier.
       - Valider avec Zod (après extraction FormData) : `employee_id` UUID, `date_debut` string, `date_fin` string, `nb_jours` number, `est_at` boolean (défaut false).
       - Si un fichier `justificatif` est fourni :
         * Vérifier type MIME : `application/pdf`, `image/jpeg`, `image/png` → sinon 400.
         * Vérifier taille ≤ 10 Mo → sinon 400.
         * Uploader dans Supabase Storage bucket `documents` au path `documents/{company_id}/{employee_id}/Médical/{timestamp}_{filename}`.
         * Récupérer l'URL publique.
         * Insérer avec `est_justifie: true` + `justificatif_url: url`.
       - Si pas de fichier : insérer avec `est_justifie: false`.
       - Type toujours `arret_maladie`. Statut initial `en_attente`.
       - `company_id` via `get_user_company_id()` (RLS). Auth obligatoire (401).
       - Retourner 201 + l'objet inséré.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compile sans erreur. Les 3 nouvelles colonnes sont dans types/supabase.ts. L'API retourne 201 sur un POST valide sans fichier.</done>
</task>

<task type="auto">
  <name>Task 2 : ArretMaladieDialog + intégration page congés</name>
  <files>components/rh/ArretMaladieDialog.tsx, app/(dashboard)/conges/page.tsx</files>
  <action>
    1. Créer `components/rh/ArretMaladieDialog.tsx` ("use client") :
       - Schéma Zod :
         ```typescript
         const schema = z.object({
           employee_id: z.string().uuid("Sélectionnez un employé"),
           date_debut: z.string().min(1, "Date de début obligatoire"),
           date_fin: z.string().min(1, "Date de fin obligatoire"),
           nb_jours: z.string().min(1),
           est_at: z.boolean().default(false),
           commentaire: z.string().max(500).optional(),
           justificatif: z.instanceof(File)
             .refine(f => f.size <= 10 * 1024 * 1024, "Max 10 Mo")
             .refine(f => ["application/pdf","image/jpeg","image/png"].includes(f.type), "PDF, JPEG ou PNG")
             .optional(),
         })
         ```
       - Formulaire Dialog shadcn avec :
         * Select employé
         * Dates début/fin + nb_jours calculé automatiquement à partir des dates (en jours ouvrables, simplifié = diff en jours calendaires)
         * Checkbox "Accident de travail (AT)" avec tooltip "L'AT doit faire l'objet d'une déclaration CNPS séparée"
         * Upload fichier : `<input type="file" accept=".pdf,.jpg,.jpeg,.png" />` avec label "Certificat médical (optionnel)"
         * Champ Commentaire optionnel
       - Soumettre via `FormData` (pas JSON) vers `POST /api/conges/arret`.
       - Toast succès "Arrêt maladie déclaré" + `router.refresh()`. Toast erreur si 4xx/5xx.
       - Désactiver le bouton pendant l'upload.
       - Props : `{ employees: Employee[] }`.

    2. Mettre à jour `app/(dashboard)/conges/page.tsx` :
       - Ajouter le bouton "Déclarer un arrêt" à côté du bouton existant `CongesDialog`, qui ouvre `ArretMaladieDialog`.
       - Dans la liste des congés, afficher des badges supplémentaires pour les arrêts maladie :
         * Si `type === "arret_maladie"` et `est_at === true` : badge "AT" (orange)
         * Si `type === "arret_maladie"` et `est_justifie === true` et `est_at === false` : badge "Justifié" (vert)
         * Si `type === "arret_maladie"` et `est_justifie === false` : badge "Non justifié" (rouge)
       - Ajouter une note informative dans la section : "Les arrêts maladie justifiés ne génèrent pas de retenue salariale. Pour les absences non justifiées, saisir les jours dans le bulletin de paie."
       - Sélectionner aussi les colonnes `est_justifie, est_at, justificatif_url` dans la requête Supabase de la page.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compile sans erreur. Le bouton "Déclarer un arrêt" ouvre ArretMaladieDialog. La soumission sans fichier crée un arrêt avec `est_justifie=false`. La soumission avec un PDF crée un arrêt avec `est_justifie=true` et `justificatif_url` renseigné. Les badges AT/Justifié/Non-justifié s'affichent dans la liste.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zéro erreur TypeScript
2. POST `/api/conges/arret` sans fichier → 201, `est_justifie=false`
3. POST `/api/conges/arret` avec PDF valide → 201, `est_justifie=true`, `justificatif_url` non null
4. POST avec fichier > 10 Mo → 400
5. Post avec MIME invalide (ex: .docx) → 400
6. La page `/conges` affiche les badges AT/Justifié/Non-justifié sur les arrêts
7. Note informative présente dans la page (pas de retenue automatique si justifié)
</verification>

<success_criteria>
- Formulaire ArretMaladieDialog fonctionnel avec upload optionnel
- `est_justifie=true` quand un justificatif est fourni et uploadé avec succès
- `est_at=true` quand la case AT est cochée
- Badges visuels distincts dans la liste congés (AT / Justifié / Non-justifié)
- Note UX guidant le RH sur l'impact bulletin des arrêts justifiés vs non-justifiés
- Fichiers stockés dans Storage bucket `documents` au path `Médical/`
- Zéro `any` TypeScript
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-conges-absences/03-05-SUMMARY.md` avec :
- Structure FormData envoyée à /api/conges/arret
- Path Storage utilisé pour les justificatifs
- Règles d'impact bulletin documentées (v1 : manuel pour RH)
- Lien vers Phase 6 : les AT déclarés ici serviront de base à QHS-01
</output>
