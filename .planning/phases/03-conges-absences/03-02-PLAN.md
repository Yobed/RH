---
phase: 03-conges-absences
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - supabase/migrations/20260401010000_conges_workflow.sql
  - app/api/conges/[id]/route.ts
  - components/rh/CongesApprovalButton.tsx
  - app/(dashboard)/conges/page.tsx
  - types/supabase.ts
autonomous: true
requirements:
  - CON-02

must_haves:
  truths:
    - "Une demande soumise a le statut 'en_attente' (plus 'demande')"
    - "Un manager peut passer le statut à 'validé_manager'"
    - "Un RH peut passer de 'validé_manager' à 'approuve' ou 'refuse'"
    - "Un refus à n'importe quelle étape passe directement à 'refuse'"
    - "La fiche du congé trace la date et l'auteur de chaque validation"
  artifacts:
    - path: "supabase/migrations/20260401010000_conges_workflow.sql"
      provides: "Colonnes statut étendu + colonnes audit workflow sur table conges"
    - path: "app/api/conges/[id]/route.ts"
      provides: "PUT avec logique de transition de statut + vérification rôle"
    - path: "components/rh/CongesApprovalButton.tsx"
      provides: "Boutons contextuels selon statut courant et rôle utilisateur"
  key_links:
    - from: "components/rh/CongesApprovalButton.tsx"
      to: "PUT /api/conges/[id]"
      via: "fetch avec body { statut, action }"
    - from: "app/(dashboard)/conges/page.tsx"
      to: "leave_balances"
      via: "colonne 'jours_pris' mise à jour automatiquement à l'approbation RH finale"
---

<objective>
Mettre en place le workflow de validation multi-niveaux (employé → manager → RH) en étendant le statut de la table `conges` et en adaptant les composants UI pour afficher les actions contextuelles selon l'étape courante.

Purpose: La conformité légale CI et l'auditabilité RH exigent que chaque approbation soit tracée avec date et auteur. Un bouton "Approuver" unique ne suffit pas.
Output: Migration SQL, API PUT enrichie, `CongesApprovalButton` multi-rôle, page congés affichant les deux files d'attente (manager / RH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-conges-absences/03-CONTEXT.md
@.planning/phases/03-conges-absences/03-01-SUMMARY.md

<interfaces>
<!-- Statuts étendus à implémenter -->
```typescript
type StatutConge =
  | "en_attente"        // soumis par l'employé (remplace "demande")
  | "valide_manager"    // approuvé niveau 1
  | "approuve"          // approuvé niveau 2 (RH) — statut final positif
  | "refuse"            // refusé à n'importe quelle étape
```

<!-- Nouvelles colonnes à ajouter sur conges -->
```sql
validated_by_manager_id UUID REFERENCES profiles(id)
validated_by_manager_at TIMESTAMPTZ
validated_by_rh_id UUID REFERENCES profiles(id)
validated_by_rh_at TIMESTAMPTZ
refus_motif TEXT
```

<!-- API PUT attendue -->
// Body: { action: "valider_manager" | "valider_rh" | "refuser", motif?: string }
// Transitions autorisées :
//   en_attente   + valider_manager → valide_manager
//   valide_manager + valider_rh    → approuve  (jours_pris mis à jour dans leave_balances)
//   en_attente | valide_manager + refuser → refuse
// Toute autre transition → 400 "Transition de statut invalide"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Migration SQL — statut étendu + colonnes audit workflow</name>
  <files>supabase/migrations/20260401010000_conges_workflow.sql, types/supabase.ts</files>
  <action>
    1. Créer `supabase/migrations/20260401010000_conges_workflow.sql` :
       ```sql
       -- Renommer "demande" → "en_attente" pour les existants
       UPDATE conges SET statut = 'en_attente' WHERE statut = 'demande';

       -- Ajouter colonnes audit (IF NOT EXISTS pour idempotence)
       ALTER TABLE conges
         ADD COLUMN IF NOT EXISTS validated_by_manager_id UUID REFERENCES profiles(id),
         ADD COLUMN IF NOT EXISTS validated_by_manager_at TIMESTAMPTZ,
         ADD COLUMN IF NOT EXISTS validated_by_rh_id UUID REFERENCES profiles(id),
         ADD COLUMN IF NOT EXISTS validated_by_rh_at TIMESTAMPTZ,
         ADD COLUMN IF NOT EXISTS refus_motif TEXT;

       -- Contrainte CHECK sur les valeurs de statut
       ALTER TABLE conges
         ADD CONSTRAINT conges_statut_check
         CHECK (statut IN ('en_attente','valide_manager','approuve','refuse'));
       ```
    2. Mettre à jour `types/supabase.ts` : ajouter les 5 nouvelles colonnes dans `conges.Row`, `conges.Insert`, `conges.Update`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compile sans erreur. Les 5 nouvelles colonnes sont typées dans supabase.ts.</done>
</task>

<task type="auto">
  <name>Task 2 : API PUT transitions + CongesApprovalButton + page congés</name>
  <files>app/api/conges/[id]/route.ts, components/rh/CongesApprovalButton.tsx, app/(dashboard)/conges/page.tsx</files>
  <action>
    1. Réécrire `app/api/conges/[id]/route.ts` :
       - Récupérer le congé existant + vérifier `company_id` RLS.
       - Parser body : `{ action: "valider_manager" | "valider_rh" | "refuser", motif?: string }`.
       - Implémenter la machine à états :
         * `en_attente` + `valider_manager` → `{ statut: "valide_manager", validated_by_manager_id: user.id, validated_by_manager_at: new Date().toISOString() }`
         * `valide_manager` + `valider_rh` → `{ statut: "approuve", validated_by_rh_id: user.id, validated_by_rh_at: now }` **ET** upsert dans `leave_balances` : `jours_pris += conge.nb_jours` pour l'année de `date_debut`.
         * `en_attente | valide_manager` + `refuser` → `{ statut: "refuse", refus_motif: motif ?? null }`
         * Toute autre combinaison → 400 `{ error: "Transition de statut invalide" }`.
       - Zod pour valider le body. Aucun `any`. Retourner le congé mis à jour.

    2. Réécrire `components/rh/CongesApprovalButton.tsx` ("use client") :
       - Props : `{ congeId: string; statut: string; canManagerApprove: boolean; canRhApprove: boolean }`.
       - Si `statut === "en_attente"` et `canManagerApprove` : bouton "Valider (Manager)" → POST action `valider_manager`.
       - Si `statut === "valide_manager"` et `canRhApprove` : bouton "Approuver (RH)" → POST action `valider_rh`.
       - Bouton "Refuser" (rouge) visible si statut `en_attente | valide_manager` : ouvre un `AlertDialog` shadcn avec champ motif optionnel.
       - Statuts terminaux (`approuve | refuse`) : afficher un badge, pas de bouton.
       - Loading state pendant fetch. Toast succès/erreur (sonner).

    3. Mettre à jour `app/(dashboard)/conges/page.tsx` :
       - Remplacer le filtre `statut === "demande"` par `["en_attente", "valide_manager"]`.
       - Séparer en deux sections : "En attente de validation manager" (`en_attente`) et "En attente de validation RH" (`valide_manager`).
       - Passer `statut` au composant `CongesApprovalButton` (rôle manager/RH simplifiés en v1 : tous les RH voient les deux files).
       - Afficher le motif de refus dans l'historique si `refus_motif` renseigné.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compile sans erreur. La page `/conges` affiche deux sections distinctes. Un congé `en_attente` passe à `valide_manager` puis à `approuve` via les deux boutons. Le refus depuis `en_attente` passe directement à `refuse`. Le `leave_balances.jours_pris` est incrémenté après approbation RH finale.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zéro erreur
2. Test manuel workflow complet : soumettre → valider manager → valider RH → vérifier `leave_balances.jours_pris` incrémenté
3. Test refus depuis `en_attente` : statut `refuse`, motif enregistré
4. Test transition invalide : PUT avec `{ action: "valider_rh" }` depuis `en_attente` → 400
</verification>

<success_criteria>
- Workflow 3 étapes fonctionnel : en_attente → valide_manager → approuve (ou refuse à chaque étape)
- Colonnes audit renseignées (id + timestamp) à chaque transition
- `leave_balances.jours_pris` mis à jour uniquement lors de l'approbation RH finale
- `CongesApprovalButton` affiche les actions correctes selon le statut courant
- Zéro `any` TypeScript
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-conges-absences/03-02-SUMMARY.md` avec :
- Détail de la machine à états implémentée
- Interface du body PUT /api/conges/[id]
- Props finales de CongesApprovalButton
</output>
