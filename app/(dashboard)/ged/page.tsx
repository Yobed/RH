export const dynamic = 'force-dynamic';
export const metadata = { title: "GED — RH Manager CI" };

// La GED réutilise le composant Archives — même contenu, URL distincte
export { default } from '@/app/(dashboard)/archives/page';
